---
phase: 02-compatibility-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .planning/codebase/STACK.md
autonomous: false
requirements:
  - UPGRADE-01
  - UPGRADE-02
  - UPGRADE-03

must_haves:
  truths:
    - "package.json declares sn-plugin-lib at ^0.1.43 (D-01)."
    - "package-lock.json resolves sn-plugin-lib to exactly 0.1.43; the cosmetic name/version 0.2.1 → 0.3.0 lockfile drift is also synced in the same npm install (D-07)."
    - "node -p \"require('sn-plugin-lib/package.json').version\" prints 0.1.43 — the working tree's node_modules has the new lib (UPGRADE-01)."
    - "npx tsc --noEmit exits 0 on the upgrade branch under strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes — re-confirms Phase 1's ghost-tsc evidence on the real branch (D-05, UPGRADE-02)."
    - "npx jest reports Tests: 87 passed, 87 total on the upgrade branch — re-confirms Phase 1's ghost-jest evidence on the real branch (D-05, UPGRADE-03)."
    - "npx eslint src/ App.tsx index.js __tests__/ exits 0 (D-05)."
    - "npx prettier --check exits 0 across src, App.tsx, index.js, __tests__ (D-05)."
    - "npm run build exits 0 and produces build/outputs/SnAlign.snplg as a non-empty file (D-05)."
    - ".planning/codebase/STACK.md lines 41 and 60 reflect ^0.1.43 (resolved 0.1.43 in node_modules) on line 41 and ^0.1.43 on line 60; no remaining ^0.1.19 references in either spot (D-08)."
    - "Current branch is chore/ricardo/02-sn-plugin-lib-upgrade, branched off latest dev (D-09)."
    - "Two distinct commits exist on the branch: a chore commit for package.json + package-lock.json, then a docs commit for .planning/codebase/STACK.md — in that order (D-10)."
    - "@babel/preset-env is resolved to 7.29.5 in package-lock.json as a within-caret freebie of the same npm install (D-02)."
    - "React (19.0.0) and React Native (0.79.2) remain locked at the Supernote PluginHost runtime versions; package.json shows no version drift for react / react-native / @react-native/* keys (D-03)."
    - "No defensive structural-type-stub audit task is included — Phase 1's ghost-jest 87/87 pass is dispositive (D-04). The plan's files_modified excludes src/ entirely."
    - "No on-device sideload step is performed in Phase 2 — UPGRADE-04 sideload-verification is explicitly deferred to Phase 4 per the ROADMAP and Task 5 hand-off (D-06)."
  artifacts:
    - path: "package.json"
      provides: "Caret-pinned sn-plugin-lib at ^0.1.43"
      contains: "\"sn-plugin-lib\": \"^0.1.43\""
    - path: "package-lock.json"
      provides: "Resolved 0.1.43 lockfile with synced cosmetic drift"
      contains: "\"version\": \"0.1.43\""
    - path: "node_modules/sn-plugin-lib/package.json"
      provides: "Working-tree node_modules now at 0.1.43"
      contains: "\"version\": \"0.1.43\""
    - path: ".planning/codebase/STACK.md"
      provides: "Brownfield doc reflects the upgraded lib version on lines 41 and 60"
      contains: "sn-plugin-lib `^0.1.43` (resolved 0.1.43 in `node_modules`)"
    - path: "build/outputs/SnAlign.snplg"
      provides: "Build artifact proving the build pipeline still works on the new lib"
      min_lines: 1
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install reconciles the manifest into the lockfile"
      pattern: "sn-plugin-lib"
    - from: "package-lock.json"
      to: "node_modules/sn-plugin-lib/"
      via: "npm install materialises the resolved version on disk"
      pattern: "0.1.43"
    - from: ".planning/codebase/STACK.md"
      to: "package.json"
      via: "The brownfield doc's recorded version string mirrors the manifest"
      pattern: "\\^0.1.43"
---

<objective>
Land `sn-plugin-lib` at `^0.1.43` on a `chore/ricardo/02-sn-plugin-lib-upgrade` branch off `dev`, refresh the lockfile (including the cosmetic `name`/`version: 0.2.1 → 0.3.0` drift per D-07 and the within-caret `@babel/preset-env: 7.29.3 → 7.29.5` patch per D-02), update `.planning/codebase/STACK.md` lines 41 and 60 to reflect the new resolved version (D-08), and prove all 5 local CI gates (eslint, prettier, tsc, jest, npm run build) stay green per D-05. Two distinct commits per D-10: a `chore(02): ...` for the dependency + lockfile churn, then a separate `docs(02): ...` for the STACK.md edit.

Purpose: ROADMAP Phase 2 success criteria #1-#4 demand a green CI-gate state on the new lib. Phase 1's audit (`.planning/research/lib-0.1.19-to-0.1.43-audit.md` §1, §2, §3 B-01) already proved the upgrade is mechanically safe — 10 of 11 call sites `unchanged`, the 11th (`resizeLassoRect` rename) already migrated via the prior 0.1.34 transitive resolution. Phase 2 re-confirms the green state on the real upgrade branch with the real lockfile (D-05), so the surface area that ROADMAP success criteria measure is observably TRUE on `dev` after merge.

Output: A `chore/ricardo/02-sn-plugin-lib-upgrade` branch on the local repo containing two commits — one bumping `package.json` + refreshing `package-lock.json` to `0.1.43`, one updating `.planning/codebase/STACK.md` lines 41 and 60 — with all 5 local CI gates passing. The branch is left ready for a `git push -u origin chore/ricardo/02-sn-plugin-lib-upgrade` and a PR to `dev` per D-09; pushing/opening the PR is outside the executor's automated scope and is the user's final action.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-compatibility-upgrade/02-CONTEXT.md
@.planning/research/lib-0.1.19-to-0.1.43-audit.md
@.planning/research/lib-0.1.43-ghost-tsc.log
@.planning/research/lib-0.1.43-ghost-jest.log
@.planning/phases/01-api-diff-audit/01-VERIFICATION.md
@.planning/codebase/STACK.md
@.claude/skills/git/SKILL.md
@CLAUDE.md
@package.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upgrade branch off latest dev</name>
  <files>(no files modified yet — branch creation only)</files>
  <read_first>
    - .claude/skills/git/SKILL.md (branch naming `<type>/<user>/<short-desc>`, target dev, prune-merged-locals snippet)
    - .planning/phases/02-compatibility-upgrade/02-CONTEXT.md §"Branch / Commit / PR Shape" (D-09 exact branch name; D-10 commit sequencing)
  </read_first>
  <action>
    Switch to `dev` and pull the latest:
    `git checkout dev && git pull origin dev`

    Run the prune-merged-locals snippet from `.claude/skills/git/SKILL.md` lines 42-47 verbatim (the `for branch in $(git branch | grep -v -E '^\*|dev|main' ...) ... gh pr list --state merged ...` block) to clean up squash-merged local branches.

    Create the upgrade branch with the exact name mandated by D-09:
    `git checkout -b chore/ricardo/02-sn-plugin-lib-upgrade`

    Verify the current branch matches D-09 and the working tree is clean of any uncommitted edits to the files this plan will touch (package.json, package-lock.json, .planning/codebase/STACK.md). Untracked `.claude/` scaffolding files visible in `git status` are pre-existing GSD tooling unrelated to this PR — do not stage them.
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "chore/ricardo/02-sn-plugin-lib-upgrade" &amp;&amp; git diff --quiet -- package.json package-lock.json .planning/codebase/STACK.md</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` outputs exactly `chore/ricardo/02-sn-plugin-lib-upgrade`.
    - `git rev-parse chore/ricardo/02-sn-plugin-lib-upgrade` and `git rev-parse dev` are identical (branch is at the tip of dev, no commits added yet).
    - `git diff -- package.json package-lock.json .planning/codebase/STACK.md` is empty (no uncommitted edits to plan-touched files).
    - `git log --oneline -1 origin/dev` (if origin/dev exists locally) matches `git log --oneline -1 dev` (the local dev is up to date with origin/dev).
  </acceptance_criteria>
  <done>Upgrade branch created off latest `dev` with the exact D-09 name; working tree clean of edits to plan-touched files.</done>
</task>

<task type="auto">
  <name>Task 2: Bump sn-plugin-lib to ^0.1.43 and refresh lockfile</name>
  <files>
    - package.json
    - package-lock.json
    - node_modules/ (gitignored; refreshed in-place)
  </files>
  <read_first>
    - package.json (current state: `"sn-plugin-lib": "^0.1.19"` on line 17; `name: "SnAlign"`, `version: "0.3.0"` on lines 2-3)
    - package-lock.json (top of file: confirm the cosmetic `name`/`version: 0.2.1` drift exists before the install runs)
    - .planning/phases/02-compatibility-upgrade/02-CONTEXT.md §"Dependency Pin Strategy" (D-01 caret pin; D-02 babel freebie; D-07 lockfile drift sync)
    - .planning/research/lib-0.1.19-to-0.1.43-audit.md §1 (ghost-evidence summary — tsc clean, jest 87/87)
  </read_first>
  <action>
    Bump the caret pin to `^0.1.43` (D-01). Use `npm pkg set` so the JSON is rewritten in the npm-canonical format (no incidental key reordering):

    `npm pkg set dependencies.sn-plugin-lib='^0.1.43'`

    Verify the manifest line changed before installing:
    `grep -n '"sn-plugin-lib"' package.json` should print `    "sn-plugin-lib": "^0.1.43"` at line 17 (position unchanged).

    Now resolve the new version into the lockfile and `node_modules`, with `--ignore-scripts` to neutralise any postinstall hooks (security hygiene; this is a dependency upgrade, not a fresh-clone setup):

    `npm install --ignore-scripts`

    Per D-07 this run also syncs the cosmetic `name: "snalign"` / `version: "0.2.1"` lockfile-root drift that npm carries forward from a stale install — npm rewrites the lockfile root to match `package.json` (`"SnAlign"` / `"0.3.0"`) automatically when it touches the lockfile.

    Per D-02 the same `npm install` picks up the within-caret `@babel/preset-env: 7.29.3 → 7.29.5` patch transparently.

    Confirm the four lockfile/resolution invariants:
    1. `node -p "require('./package.json').dependencies['sn-plugin-lib']"` prints `^0.1.43`.
    2. `node -p "require('sn-plugin-lib/package.json').version"` prints `0.1.43` (working-tree node_modules now at 0.1.43).
    3. `node -e "const lock=require('./package-lock.json'); console.log(lock.name, lock.version)"` prints `SnAlign 0.3.0` (cosmetic drift fixed per D-07).
    4. `node -e "const lock=require('./package-lock.json'); console.log(lock.packages['node_modules/sn-plugin-lib'].version)"` prints `0.1.43`.

    Commit the bump + lockfile in a single chore commit per D-10. Stage ONLY the two files (do NOT use `git add -A` — the working tree has untracked `.claude/` scaffolding that must not enter this PR):

    `git add package.json package-lock.json`
    `git commit -m "chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile"`

    Confirm only the two intended files are in the commit:
    `git show --stat HEAD` should list `package.json` and `package-lock.json` only.
  </action>
  <verify>
    <automated>test "$(node -p "require('./package.json').dependencies['sn-plugin-lib']")" = "^0.1.43" &amp;&amp; test "$(node -p "require('sn-plugin-lib/package.json').version")" = "0.1.43" &amp;&amp; test "$(node -e "const l=require('./package-lock.json'); console.log(l.name+' '+l.version)")" = "SnAlign 0.3.0" &amp;&amp; test "$(node -e "const l=require('./package-lock.json'); console.log(l.packages['node_modules/sn-plugin-lib'].version)")" = "0.1.43" &amp;&amp; test "$(git show --stat HEAD --format='' | grep -cE '^ (package\.json|package-lock\.json) ')" = "2" &amp;&amp; test "$(git log -1 --format=%s)" = "chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"sn-plugin-lib": "\^0.1.19"' package.json` outputs `0` AND `grep -c '"sn-plugin-lib": "\^0.1.43"' package.json` outputs `1`.
    - `node -p "require('sn-plugin-lib/package.json').version"` outputs `0.1.43`.
    - `node -e "const l=require('./package-lock.json'); console.log(l.name+' '+l.version)"` outputs `SnAlign 0.3.0` (per D-07 the cosmetic drift is fixed).
    - `node -e "const l=require('./package-lock.json'); console.log(l.packages['node_modules/sn-plugin-lib'].version)"` outputs `0.1.43`.
    - `node -e "const l=require('./package-lock.json'); const v=Object.entries(l.packages).find(([k])=>k.endsWith('/@babel/preset-env'))?.[1]?.version; console.log(v)"` outputs a `7.29.x` value where `x >= 5` (D-02 freebie present; the exact patch is npm-resolver-determined but must be ≥ 7.29.5).
    - `git log -1 --format=%s` outputs exactly `chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile`.
    - `git show --stat HEAD` lists exactly two files: `package.json` and `package-lock.json` (no `.claude/` scaffolding, no STACK.md — that lands in Task 4).
    - `git diff --name-only HEAD~1 HEAD -- src/` is empty (no `src/` modification, per the phase boundary).
  </acceptance_criteria>
  <done>`sn-plugin-lib` is at `^0.1.43` in `package.json`; `package-lock.json` resolves it to `0.1.43`; `node_modules/sn-plugin-lib/` has the new lib; cosmetic `name`/`version` lockfile drift is gone; a single chore commit holds both files.</done>
</task>

<task type="auto">
  <name>Task 3: Run the 5 local CI gates and confirm green</name>
  <files>(no files modified — verification only; build emits build/outputs/SnAlign.snplg as a gitignored artifact)</files>
  <read_first>
    - .claude/skills/git/SKILL.md §"Run CI checks locally before opening PR" (the 5-command list, lines 73-77)
    - CLAUDE.md §"Commands" (canonical command surface)
    - .planning/research/lib-0.1.43-ghost-tsc.log (Phase 1's tsc-exit-0 ghost-evidence — Phase 2 reproduces this on the real branch)
    - .planning/research/lib-0.1.43-ghost-jest.log (Phase 1's `Tests: 87 passed, 87 total` ghost-evidence)
    - tsconfig.json (confirms strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes are the flags tsc runs under)
  </read_first>
  <action>
    Run the 5 gates from `.claude/skills/git/SKILL.md` lines 73-77 in sequence. Each must exit 0; on any non-zero, surface the failing gate's output and stop — do NOT commit STACK.md (Task 4) on a red branch, because the docs commit's premise is that the green-on-^0.1.43 truths are observable.

    Gate 1 — ESLint:
    `npx eslint src/ App.tsx index.js __tests__/`

    Gate 2 — Prettier:
    `npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts"`

    Gate 3 — TypeScript strict typecheck:
    `npx tsc --noEmit`

    Gate 4 — Jest. The git skill specifies `npx jest --coverage`; CLAUDE.md's `Commands` block specifies `npm test`. Both ultimately invoke `jest`. Run the no-coverage form so the output matches Phase 1's ghost-jest-log shape (Phase 1 ran without coverage to capture the canonical `Tests: 87 passed, 87 total` summary). Coverage is a separate concern; the gate's purpose here is the pass/fail bit:
    `npx jest`

    The output MUST contain the literal substring `87 passed, 87 total` (Jest's default formatter pads, so match flexibly — Phase 1's ghost-jest log shows `Tests:       87 passed, 87 total`).

    Gate 5 — Build:
    `npm run build`

    This invokes `npm run clean && bash buildPlugin.sh` and produces `build/outputs/SnAlign.snplg`. Confirm the artifact exists and is non-empty:
    `test -s build/outputs/SnAlign.snplg`

    Do NOT commit the `build/` directory — it is gitignored and the artifact is regenerated on every release.

    If all 5 gates pass, the branch is in the green state ROADMAP success criteria #2, #3, #4 demand. Proceed to Task 4.
  </action>
  <verify>
    <automated>npx eslint src/ App.tsx index.js __tests__/ &amp;&amp; npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts" &amp;&amp; npx tsc --noEmit &amp;&amp; npx jest 2>&amp;1 | grep -q "87 passed, 87 total" &amp;&amp; npm run build &amp;&amp; test -s build/outputs/SnAlign.snplg</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ App.tsx index.js __tests__/` exits 0 with no error output.
    - `npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts"` exits 0.
    - `npx tsc --noEmit` exits 0 under the strict flags configured in `tsconfig.json`; the run reproduces Phase 1's ghost-tsc-log evidence on the real branch (D-05).
    - `npx jest` exits 0; output contains the literal substring `87 passed, 87 total`; the run reproduces Phase 1's ghost-jest-log evidence on the real branch (D-05).
    - `npm run build` exits 0 and `build/outputs/SnAlign.snplg` exists with size > 0 bytes.
    - No new file under `src/`, `App.tsx`, `index.js`, `__tests__/` was modified during this task (`git status --porcelain -- src/ App.tsx index.js __tests__/` is empty).
    - The working tree is still on `chore/ricardo/02-sn-plugin-lib-upgrade` at the chore commit from Task 2 (`git log -1 --format=%s` still outputs `chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile`).
  </acceptance_criteria>
  <done>All 5 local CI gates pass green on the upgrade branch; `build/outputs/SnAlign.snplg` materialised; branch is in the state ROADMAP success criteria #2-#4 require.</done>
</task>

<task type="auto">
  <name>Task 4: Update STACK.md lines 41 and 60 to reflect ^0.1.43 (separate docs commit)</name>
  <files>
    - .planning/codebase/STACK.md
  </files>
  <read_first>
    - .planning/codebase/STACK.md (lines 35-65 — confirm the two exact strings to replace before editing)
    - .planning/phases/02-compatibility-upgrade/02-CONTEXT.md §"Lockfile and Brownfield-doc Housekeeping" (D-08 exact replacement strings)
    - .planning/phases/02-compatibility-upgrade/02-CONTEXT.md §"Branch / Commit / PR Shape" (D-10 — separate `docs(02):` commit for the STACK.md change)
  </read_first>
  <action>
    Two surgical Edit operations on `.planning/codebase/STACK.md` per D-08. Each is an exact-string replacement; do NOT do a global find-replace (the two lines have different surrounding text and only line 41 carries the `(resolved 0.1.34 in node_modules)` annotation).

    Edit 1 — line 41 (the §Frameworks bullet has the full resolved-version annotation). The line currently reads (preserving the markdown backticks):
    `- sn-plugin-lib ` then a backtick-wrapped `^0.1.19`, then ` (resolved 0.1.34 in ` then backtick-wrapped `node_modules`, then `) — Supernote plugin framework providing ` followed by the three backtick-wrapped API class names and the trailing INTEGRATIONS.md reference.
    Replace the two version substrings only:
    - backtick-wrapped `^0.1.19` → backtick-wrapped `^0.1.43`
    - `resolved 0.1.34 in` → `resolved 0.1.43 in`
    All other markup on the line stays byte-identical.

    Edit 2 — line 60 (the §Key Dependencies bullet has NO `(resolved ...)` annotation). The line currently reads:
    `- ` then backtick-wrapped `sn-plugin-lib`, then ` ^0.1.19 — only native bridge to the Supernote firmware. All UI affordances and lasso operations are routed through it.`
    Replace only the version substring:
    - `^0.1.19` → `^0.1.43`
    The trailing prose is byte-identical.

    Confirm the edits before committing:
    `grep -n "0\\.1\\.19" .planning/codebase/STACK.md` MUST print nothing.
    `grep -n "0\\.1\\.34" .planning/codebase/STACK.md` MUST print nothing.
    `grep -n "0\\.1\\.43" .planning/codebase/STACK.md` MUST print at least 2 distinct line matches.
    `grep -c "resolved 0\\.1\\.43 in" .planning/codebase/STACK.md` MUST be 1 (only line 41 carries that annotation).

    Commit the STACK.md change as a separate docs commit per D-10. Stage ONLY the one file:

    `git add .planning/codebase/STACK.md`
    `git commit -m "docs(02): update STACK.md to reflect sn-plugin-lib ^0.1.43"`

    Confirm the two-commit branch state:
    `git log --oneline -2` should show, in this order:
      1. (newest) `docs(02): update STACK.md to reflect sn-plugin-lib ^0.1.43`
      2. (older)  `chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile`
  </action>
  <verify>
    <automated>test "$(grep -c '0\.1\.19' .planning/codebase/STACK.md)" = "0" &amp;&amp; test "$(grep -c '0\.1\.34' .planning/codebase/STACK.md)" = "0" &amp;&amp; test "$(grep -c '0\.1\.43' .planning/codebase/STACK.md)" -ge "2" &amp;&amp; test "$(grep -c 'resolved 0\.1\.43 in' .planning/codebase/STACK.md)" = "1" &amp;&amp; test "$(git log -1 --format=%s)" = "docs(02): update STACK.md to reflect sn-plugin-lib ^0.1.43" &amp;&amp; test "$(git log -2 --format=%s | tail -1)" = "chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile" &amp;&amp; test "$(git show --stat HEAD --format='' | grep -cE '^ \.planning/codebase/STACK\.md ')" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "0\\.1\\.19" .planning/codebase/STACK.md` outputs `0` (no remaining old-version references on the two lines).
    - `grep -c "0\\.1\\.34" .planning/codebase/STACK.md` outputs `0` (old resolved-version gone).
    - `grep -c "0\\.1\\.43" .planning/codebase/STACK.md` outputs ≥ `2` (lines 41 and 60 both carry the new version).
    - `grep -c "resolved 0\\.1\\.43 in" .planning/codebase/STACK.md` outputs `1` (only line 41 carries the annotation; line 60 stays annotation-free, matching the pre-edit asymmetry).
    - `git log -1 --format=%s` outputs exactly `docs(02): update STACK.md to reflect sn-plugin-lib ^0.1.43`.
    - `git log -2 --format=%s | tail -1` outputs exactly `chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile` (the chore commit is the parent of the docs commit per D-10).
    - `git show --stat HEAD` lists exactly one file: `.planning/codebase/STACK.md` (no other files smuggled in).
    - `git diff --name-only dev HEAD` lists exactly three files: `package.json`, `package-lock.json`, `.planning/codebase/STACK.md` (the full PR changeset).
  </acceptance_criteria>
  <done>STACK.md lines 41 and 60 reflect `^0.1.43`; a separate `docs(02):` commit holds the change; the branch now has the exact two-commit graph D-10 mandates; the working tree carries only the three PR-bound files as diff against `dev`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Confirm green branch state and hand off for PR</name>
  <files>(no files modified — checkpoint hand-off; user pushes branch and opens PR to dev)</files>
  <action>
    This is a human-verify checkpoint. The executor presents the branch state and resume-signal options to the user; the user pushes the branch and opens the PR per D-09. The executor does NOT push or open the PR autonomously (per .claude/skills/git/SKILL.md "Never push directly to dev or main" and the human-action-only nature of opening a PR with curated title/body wording from CONTEXT.md §"Claude's Discretion").
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "chore/ricardo/02-sn-plugin-lib-upgrade" &amp;&amp; test "$(git rev-list --count dev..HEAD)" = "2" &amp;&amp; test "$(git log -1 --format=%s)" = "docs(02): update STACK.md to reflect sn-plugin-lib ^0.1.43" &amp;&amp; test "$(git log -2 --format=%s | tail -1)" = "chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile" &amp;&amp; test "$(node -p "require('sn-plugin-lib/package.json').version")" = "0.1.43"</automated>
  </verify>
  <done>User has confirmed branch state, pushed `chore/ricardo/02-sn-plugin-lib-upgrade` to origin, and opened a PR to `dev`. Resume signal received.</done>
  <what-built>
    A `chore/ricardo/02-sn-plugin-lib-upgrade` branch off `dev` with exactly two commits:
    1. `chore(02): bump sn-plugin-lib to ^0.1.43 and refresh lockfile` — `package.json` + `package-lock.json`.
    2. `docs(02): update STACK.md to reflect sn-plugin-lib ^0.1.43` — `.planning/codebase/STACK.md` lines 41 and 60.
    All 5 local CI gates passed at Task 3: eslint, prettier --check, tsc --noEmit, jest (`87 passed, 87 total`), `npm run build` (produced `build/outputs/SnAlign.snplg`).
  </what-built>
  <how-to-verify>
    1. Confirm the branch state and changeset:
       - `git branch --show-current` → `chore/ricardo/02-sn-plugin-lib-upgrade`
       - `git log --oneline dev..HEAD` → 2 commits, in this order top-down: docs(02), chore(02)
       - `git diff --name-only dev HEAD` → `.planning/codebase/STACK.md`, `package-lock.json`, `package.json` (3 files)
    2. Spot-check the lib version is actually live in `node_modules`:
       - `node -p "require('sn-plugin-lib/package.json').version"` → `0.1.43`
    3. (Optional, takes ~2-3 minutes) re-run the 5 CI gates from `.claude/skills/git/SKILL.md` lines 73-77 to satisfy yourself before pushing — they passed at Task 3, but a re-run on the final commit state matches what GitHub Actions will do on the PR.
    4. Push the branch and open the PR to `dev` per D-09:
       - `git push -u origin chore/ricardo/02-sn-plugin-lib-upgrade`
       - `gh pr create --base dev --title "chore(02): sn-plugin-lib compatibility upgrade — ^0.1.19 → ^0.1.43" --body "<your body — Phase 2 context, CI-gate checklist, link to .planning/research/lib-0.1.19-to-0.1.43-audit.md>"` (exact title and body wording are at Claude's discretion per CONTEXT.md §"Claude's Discretion").
    5. Reply with one of:
       - `approved` — green and PR opened; Phase 2 implementation complete on the local branch side.
       - `re-run gates` — request a re-run of `npx eslint`, `npx prettier --check`, `npx tsc --noEmit`, `npx jest`, `npm run build` before pushing.
       - free-form notes — describe any gate that failed unexpectedly, any commit-graph anomaly, or any deviation from D-01..D-10 that needs revision.
  </how-to-verify>
  <resume-signal>Type "approved" (PR opened on dev), "re-run gates" (run all 5 CI checks again), or describe an issue that requires plan revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → working tree | `npm install` fetches `sn-plugin-lib@0.1.43` (and transitive `@babel/preset-env: 7.29.5`) over HTTPS from `registry.npmjs.org` and writes the resolved tree into the gitignored `node_modules/`. The lockfile (committed) is the integrity boundary — `package-lock.json` carries SRI hashes (`integrity: "sha512-..."`) for every resolved package. |
| Local branch → origin/dev | The two commits this plan produces are pushed by the user via PR per D-09; CI on `dev` runs the same 5 gates again. No direct push to `dev` per `.claude/skills/git/SKILL.md`. |
| Build pipeline → release | `npm run build` produces `build/outputs/SnAlign.snplg`. This is verified locally in Task 3 but NOT sideloaded — sideload is Phase 4 (UPGRADE-04) per D-06. The .snplg from this branch is therefore unsigned/untested device-side until Phase 4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm install (postinstall hooks) | mitigate | Task 2 uses `npm install --ignore-scripts`. `sn-plugin-lib` is not net-new (was already a project dependency at ^0.1.19); the legitimacy gate is therefore already satisfied — this is a within-package version bump, not a new package introduction. `@babel/preset-env` is also pre-existing (within-caret patch bump per D-02). No `[ASSUMED]`/`[SUS]`/`[SLOP]` checkpoint required. |
| T-02-02 | Spoofing / Integrity | package-lock.json drift | mitigate | The lockfile is the integrity anchor — SRI hashes for `sn-plugin-lib@0.1.43` and `@babel/preset-env@7.29.5` are written by `npm install` and travel with the chore commit. Anyone checking out the branch and running `npm ci` gets the exact same bits, byte-for-byte. The acceptance criteria on Task 2 assert `lock.packages['node_modules/sn-plugin-lib'].version === '0.1.43'` to catch a regression where the lockfile didn't update. |
| T-02-03 | Elevation of Privilege | direct push to dev | mitigate | `.claude/skills/git/SKILL.md` mandates "Never push directly to `dev` or `main`. All code enters via PR." Task 1 creates the work branch off `dev`; Task 5's resume-signal hands the push + PR open over to the human. The plan executor never runs `git push origin dev`. |
| T-02-04 | Information Disclosure | secrets in commits | accept | No secrets are touched by this phase. `package.json`, `package-lock.json`, and `.planning/codebase/STACK.md` contain no credentials. `.env` is not in scope; SnAlign reads no `process.env` keys at runtime (per `.planning/codebase/STACK.md` §Configuration). No mitigation needed beyond Task 2 / Task 4 staging individual files (no `git add -A`) so untracked `.claude/` scaffolding can't leak in. |
</threat_model>

<verification>
Once all four implementation tasks (1-4) complete, the branch must satisfy every truth in `must_haves.truths` above. The Task 5 checkpoint is the human-side hand-off; it does not gate the truth set — Tasks 2 and 4's automated checks already prove the state is correct.

Cross-cut verification — these checks are redundant against per-task acceptance criteria but confirm the whole-plan state:

1. `git branch --show-current` outputs `chore/ricardo/02-sn-plugin-lib-upgrade` (D-09).
2. `git log --oneline dev..HEAD | wc -l` outputs `2` (D-10 — exactly two commits ahead of dev).
3. `git diff --name-only dev HEAD` outputs exactly three paths: `.planning/codebase/STACK.md`, `package-lock.json`, `package.json` (the phase boundary holds — no `src/` edits).
4. `grep -c '^0.1.43' package.json` outputs `1`.
5. `node -p "require('sn-plugin-lib/package.json').version"` outputs `0.1.43`.
6. `grep -c "^- sn-plugin-lib \`\\^0.1.43\` (resolved 0.1.43 in" .planning/codebase/STACK.md` outputs `1`.
7. `npx eslint src/ App.tsx index.js __tests__/ && npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts" && npx tsc --noEmit && npx jest 2>&1 | grep -q "87 passed, 87 total" && npm run build && test -s build/outputs/SnAlign.snplg` exits 0.
</verification>

<success_criteria>
Phase 2 is complete on the local branch side when:
- All four ROADMAP Phase 2 success criteria are observably TRUE on the `chore/ricardo/02-sn-plugin-lib-upgrade` branch:
  1. `package.json` declares `sn-plugin-lib@^0.1.43` and `npm ci` (a re-resolution check) would succeed with no transitive conflicts.
  2. `npx tsc --noEmit` exits 0 under strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` (Task 3 Gate 3).
  3. `npx jest` reports `87 passed, 87 total` (Task 3 Gate 4).
  4. `npx eslint` and `npx prettier --check` are green (Task 3 Gates 1-2); `npm run build` succeeds (Task 3 Gate 5) — CI's `build-check` aggregator would pass.
- D-01 through D-10 from `.planning/phases/02-compatibility-upgrade/02-CONTEXT.md` are each reflected in either an action, an acceptance criterion, or a `must_haves.truths` line of this plan (see Coverage Audit below).
- Two commits exist on the branch in the order D-10 mandates: chore(02) parent, docs(02) tip.
- The branch is left ready for the human to push and open a PR to `dev` per D-09. The push and PR-open are the user's Task 5 actions, NOT the executor's.

Phase 2 will be merged into `dev` once GitHub Actions' `build-check` passes on the PR; CI re-runs the same 5 gates Task 3 ran locally.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

### GOAL (from ROADMAP §"Phase 2: Compatibility Upgrade")
| # | Item | Covered by | Status |
|---|------|------------|--------|
| G-1 | "SnAlign builds, typechecks, and tests green on `sn-plugin-lib ^0.1.43`" | Tasks 2 + 3; truths covering package.json pin, tsc clean, jest 87/87, build success | COVERED |
| G-2 | "the codebase as it stood before any adoption work, just relinked against the new lib" | `files_modified` excludes `src/`; Task 2 + 4 acceptance criteria assert no `src/` diff vs dev | COVERED |

### REQ (phase_req_ids = UPGRADE-01, UPGRADE-02, UPGRADE-03)
| ID | Requirement summary | Plan | Task(s) | Status |
|----|---------------------|------|---------|--------|
| UPGRADE-01 | `package.json` declares `sn-plugin-lib@^0.1.43`; `npm ci` resolves cleanly; no transitive-dep conflicts | 02-01 | T2 (bump + install); T3 G4 (jest runs implies install resolved cleanly) | COVERED |
| UPGRADE-02 | `npx tsc --noEmit` passes under strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`; no `any` suppressions, no `@ts-expect-error` added | 02-01 | T3 Gate 3; T2 acceptance asserts no `src/` diff (so no new suppressions could have been introduced) | COVERED |
| UPGRADE-03 | All 87 Jest tests pass; mocks updated if SDK shape moved | 02-01 | T3 Gate 4; Phase 1 audit §2 already confirmed every call site `unchanged`, so the no-mock-edits state is correct on its own | COVERED |

### RESEARCH (key load-bearing artifacts from `.planning/research/lib-0.1.19-to-0.1.43-audit.md`)
| Item | Relevance | Covered by | Status |
|------|-----------|------------|--------|
| §1 Executive Summary — tsc clean, jest 87/87 on ghost-upgrade | Phase 2 must reproduce on real branch | T3 Gates 3-4 | COVERED (D-05) |
| §2 Call-Site Sweep — 10/11 unchanged | No `src/` edits needed | `files_modified` excludes `src/`; T2/T4 acceptance criteria assert no `src/` diff | COVERED (D-04) |
| §3 B-01 `updateLassoRect → resizeLassoRect` already migrated | No mitigation work | Audit citation in objective + T2 read_first | COVERED |
| §4 Behavioral changes (Beh-01, Beh-02) — both `paper-only`, no source edits | Phase 2 no-op | Audit cited in objective; no task needed | COVERED (informational) |
| §5 Net-new APIs (N-01..N-11) — 3 needs-eval, 8 no | Phase 3 territory | Out of scope per CONTEXT.md "Out of scope: Phase 3 spike work" | COVERED (out of phase scope; tagged informational) |

### CONTEXT (D-01..D-10 from CONTEXT.md)
| ID | Decision | Covered by | Status |
|----|----------|------------|--------|
| D-01 | Caret pin `^0.1.43` | T2 `npm pkg set ...^0.1.43`; must_haves truth #1 | COVERED |
| D-02 | `@babel/preset-env` 7.29.3 → 7.29.5 within-caret freebie | T2 acceptance: lockfile contains `@babel/preset-env` ≥ 7.29.5; must_haves truth #12 | COVERED |
| D-03 | React / RN locked at 19.0.0 / 0.79.2 | `files_modified` excludes `package.json` keys for these (T2 only touches `dependencies.sn-plugin-lib`); no version drift introduced | COVERED |
| D-04 | Trust Phase 1 ghost evidence — no defensive structural-stub audit | No audit task in plan; `files_modified` excludes `src/`; must_haves cite Phase 1 evidence (truths #4 and #5) | COVERED |
| D-05 | Re-run all 5 local CI gates on real branch | T3 (all 5 gates as separate steps); must_haves truths #4-#8 | COVERED |
| D-06 | No on-device sideload — Phase 4 territory | T5 checkpoint explicitly hands off to PR (no sideload step); `files_modified` excludes `.snplg` (build/ is gitignored) | COVERED (informational — explicit non-action) |
| D-07 | Sync cosmetic lockfile drift (`name: snalign` / `version: 0.2.1` → `SnAlign` / `0.3.0`) in same `npm install` | T2 action documents the drift sync; T2 acceptance asserts `lock.name='SnAlign'`, `lock.version='0.3.0'`; must_haves truth #2 | COVERED |
| D-08 | Update STACK.md line 41 (and matching string on line 60) | T4 (both lines edited as separate exact-string replacements); must_haves truth #9 | COVERED |
| D-09 | Branch `chore/ricardo/02-sn-plugin-lib-upgrade` → PR to dev | T1 (branch creation with exact name); T5 (PR open hand-off); must_haves truth #10 | COVERED |
| D-10 | Single chore bump commit + separate docs commit (in that order) | T2 (chore commit); T4 (docs commit, parent = chore); T4 acceptance asserts `git log -2` order; must_haves truth #11 | COVERED |

### Exclusions (not gaps)
- Phase 3 needs-eval items (N-01 `lassoElements`, N-03 `generateLassoPreview`, N-04 `showPluginView`) — explicit Phase 3 territory per ROADMAP.
- Phase 4 sideload (UPGRADE-04) — explicit Phase 4 territory per D-06.
- Phase 4 SKILL-01..05 (`~/.claude/skills/sn-plugin/` edits) — explicit Phase 4 territory per CONTEXT.md "Out of scope".
- Deferred Ideas in CONTEXT.md (TypeScript 6, Jest 30, ESLint 10, RN cli 20, React patch bump, `@react-native/*` 0.84.1, defensive structural-stub audit) — all explicitly tagged Deferred per CONTEXT.md §"Deferred Ideas"; captured for future REQUIREMENTS.md v2 milestone.

### Audit result
All GOAL items, all REQ IDs (UPGRADE-01/02/03), all in-scope RESEARCH items, and all D-01..D-10 CONTEXT decisions are COVERED. No unplanned items. No phase split required.
</source_audit>

<output>
Create `.planning/phases/02-compatibility-upgrade/02-01-SUMMARY.md` once execution completes, summarising the chore + docs commits, the green CI-gate evidence on the real branch (with paths to any captured logs alongside Phase 1's `.planning/research/lib-0.1.43-ghost-{tsc,jest}.log`), the final `package-lock.json` resolved versions for `sn-plugin-lib` and `@babel/preset-env`, and the PR URL once Task 5's hand-off completes.
</output>
