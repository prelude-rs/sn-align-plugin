---
phase: 01-api-diff-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .planning/research/lib-0.1.19-to-0.1.43-audit.md
autonomous: true
requirements:
  - UPGRADE-05

must_haves:
  truths:
    - "The audit doc exists at .planning/research/lib-0.1.19-to-0.1.43-audit.md with a clear H1 title, table of contents, and three top-level sections: Breaking Changes, Behavioral Changes, Net-new APIs."
    - "Each of the 7 SnAlign call sites named in ROADMAP success criterion #4 appears in the audit — either with a concrete change entry OR with an explicit 'no change' line per D-12. Audit scope follows D-06 (call sites + 4 targeted answers + adjacent-domain net-new APIs); unrelated domains get a single 'present, not surveyed' line."
    - "Every Breaking entry includes file/line citation to node_modules/sn-plugin-lib/src/** or lib/typescript/src/** + a `Phase 2 action:` line per D-13."
    - "Every Behavioral entry includes the same citation discipline + a `Phase 2 action:` line; entries where types don't surface the change are explicitly tagged `paper-only`."
    - "Every Net-new entry includes a `Phase 3 candidate:` flag (`yes` / `no` / `needs-eval`) per D-13."
    - "Strict-TS-only failures (clean under stock TS but failing under noUncheckedIndexedAccess / exactOptionalPropertyTypes / strict) are classified as Breaking per D-11 with the failing flag named."
    - "Removed / deprecated APIs are documented even when SnAlign doesn't currently call them, with `SnAlign-impact: None — not currently called` per D-12."
  artifacts:
    - path: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      provides: "Primary audit consumable for Phase 2/3/4; the call-site sweep and three classification buckets."
      min_lines: 80
      contains: "## Breaking Changes"
  key_links:
    - from: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      to: ".planning/research/lib-0.1.43-ghost-tsc.log"
      via: "Each Breaking entry's citation block references the line in ghost-tsc.log where the error appears."
      pattern: "ghost-tsc.log"
    - from: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      to: ".planning/research/lib-0.1.19-to-0.1.43-dts.diff"
      via: "Behavioral and Net-new entries cite the dts.diff hunk that surfaces the type change."
      pattern: "dts.diff"
---

<objective>
Write the bulk of the audit document: the three classification buckets (Breaking / Behavioral / Net-new) and the call-site sweep for each SnAlign integration point. This is the load-bearing artifact UPGRADE-05 demands and the Phase 2/3/4 contract per D-17.

Purpose: ROADMAP success criteria #1 + #4 land in this plan. Each existing SnAlign call site (`PluginManager.{init, registerButton, registerButtonListener, getPluginDirPath, closePluginView}`, `PluginCommAPI.{getLassoRect, resizeLassoRect, setLassoBoxState, getCurrentFilePath, getCurrentPageNum}`, `PluginFileAPI.getPageSize`) is checked against the 0.1.43 types; everything that changed lands in Breaking or Behavioral with a `Phase 2 action:` line; everything newly added in adjacent domains lands in Net-new with a `Phase 3 candidate:` flag.

Output: a substantial first version of `.planning/research/lib-0.1.19-to-0.1.43-audit.md` covering the three buckets. The "4 targeted answers" section is left as a stub heading to be filled by Plan 04. The Unknowns / skill-cross-reference sections are stubs filled by Plan 05.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-diff-audit/01-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
@.planning/research/lib-0.1.19-to-0.1.43-dts.diff
@.planning/research/lib-0.1.43-ghost-tsc.log
@.planning/research/lib-0.1.43-ghost-jest.log
@src/handlers/onLassoMain.ts
@src/sdk/pageSize.ts
@src/buttons/buttonCommon.ts
@src/buttons/registerLassoButton.ts
@src/sdk/closeView.ts
@src/sdk/types.ts
@index.js

<interfaces>
<!-- SnAlign-side structural-type contracts the audit must validate against the new 0.1.43 types. -->
<!-- Extracted from the brownfield INTEGRATIONS.md and the *Like types in src/. -->

From src/sdk/types.ts:
- `APIResponse<T> = { success: boolean; result?: T; error?: { code: number; message: string } }`

From src/buttons/buttonCommon.ts (PluginManagerLike):
- `init(): Promise<void> | void`
- `registerButton(type, appTypes, button): Promise<APIResponse<unknown>>`
- `registerButtonListener(listener): void`
- `setButtonState(id, enabled): Promise<APIResponse<unknown>>`
- `modifyButtonRes?(...)` — optional; firmware-unreliable
- `getPluginDirPath(): Promise<string | null | undefined>`
- `closePluginView(): Promise<boolean> | boolean`

From src/handlers/onLassoMain.ts (LassoCommAPILike):
- `getLassoRect(): Promise<APIResponse<Rect>>`
- `resizeLassoRect(rect: Rect): Promise<APIResponse<unknown>>`
- `setLassoBoxState(state: number): Promise<APIResponse<unknown>>`
- `getCurrentFilePath(): Promise<APIResponse<string>>`  (used via PageSizeCommAPI shim too)
- `getCurrentPageNum(): Promise<APIResponse<number>>`

From src/sdk/pageSize.ts (PageSizeFileAPI):
- `getPageSize(filePath: string, pageNum: number): Promise<APIResponse<{width: number; height: number}>>`

Audit task = for each row above, verify the 0.1.43 type still matches the SnAlign assumption, OR flag the mismatch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the audit doc structure and run the call-site sweep</name>
  <files>.planning/research/lib-0.1.19-to-0.1.43-audit.md</files>
  <read_first>
    - .planning/codebase/INTEGRATIONS.md §"Supernote Firmware SDK (`sn-plugin-lib`)" (the canonical SnAlign call-site inventory — every row in those tables MUST be revisited)
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Net-new API Scope" (D-06 call sites + 4 targeted + adjacent; D-12 removed/deprecated documented even if unused)
    - .planning/research/lib-0.1.43-ghost-tsc.log (the primary signal — every error here becomes a Breaking entry per D-11)
    - .planning/research/lib-0.1.43-ghost-jest.log (failing tests indicate behavioral drift not caught by types)
    - .planning/research/lib-0.1.19-to-0.1.43-dts.diff (coverage for APIs we don't currently call)
    - node_modules/sn-plugin-lib/src/ (source of truth per D-03 — read the relevant files when the diff or types are ambiguous)
    - node_modules/sn-plugin-lib/lib/typescript/src/ (types — secondary authority)
    - src/handlers/onLassoMain.ts (SnAlign call sites with line numbers; cite these when writing `Phase 2 action:` lines)
    - src/sdk/pageSize.ts
    - src/buttons/registerLassoButton.ts
    - src/buttons/buttonCommon.ts
    - src/sdk/closeView.ts
    - index.js (the two real imports + the lassoDeps wiring at lines 16-39)
  </read_first>
  <action>
    Create `.planning/research/lib-0.1.19-to-0.1.43-audit.md` with the following SECTION STRUCTURE (heading text must match for downstream grep gates):

    ```
    # sn-plugin-lib 0.1.19 → 0.1.43 API Diff Audit

    Date: 2026-05-17
    Authority order (per D-03): source > types > docs.
    Evidence:
    - Machine .d.ts diff: ./lib-0.1.19-to-0.1.43-dts.diff
    - Ghost-upgrade tsc log: ./lib-0.1.43-ghost-tsc.log
    - Ghost-upgrade jest log: ./lib-0.1.43-ghost-jest.log

    ## Table of Contents
    1. Executive Summary
    2. Call-Site Sweep (SnAlign-touched APIs)
    3. Breaking Changes
    4. Behavioral Changes
    5. Net-new APIs
    6. Four Targeted Answers (filled in Plan 04)
    7. sn-plugin Skill Gotcha Cross-Reference (filled in Plan 05)
    8. Unknowns / Paper-only Claims (filled in Plan 05)

    ## 1. Executive Summary
    (one-paragraph headline: did the upgrade compile? how many test failures? how many type-surface changes touch SnAlign call sites?)

    ## 2. Call-Site Sweep

    For each of the 7 SnAlign-touched APIs from INTEGRATIONS.md, a row stating: 0.1.19 signature → 0.1.43 signature, verdict (`unchanged` / `breaking` / `behavioral` / `paper-only-deferred`), and either a `Phase 2 action:` line OR an explicit "No change — SnAlign safe."

    Required rows (one row per API; missing any row = audit incomplete per ROADMAP criterion #4):
    - PluginManager.init  — index.js:16
    - PluginManager.registerButton  — src/buttons/registerLassoButton.ts:32
    - PluginManager.registerButtonListener  — src/buttons/registerLassoButton.ts:53
    - PluginManager.getPluginDirPath  — src/buttons/buttonCommon.ts:30
    - PluginManager.closePluginView  — index.js:35, src/ui/PopupRoot.tsx:27
    - PluginCommAPI.getLassoRect  — src/handlers/onLassoMain.ts:60
    - PluginCommAPI.resizeLassoRect  — src/handlers/onLassoMain.ts:181
    - PluginCommAPI.setLassoBoxState  — src/handlers/onLassoMain.ts:69
    - PluginCommAPI.getCurrentFilePath  — src/sdk/pageSize.ts:29
    - PluginCommAPI.getCurrentPageNum  — src/sdk/pageSize.ts:29
    - PluginFileAPI.getPageSize  — src/sdk/pageSize.ts:36
    ```

    For each row in §2:
    1. Locate the 0.1.43 declaration in `node_modules/sn-plugin-lib/lib/typescript/src/**/*.d.ts` AND the implementation in `node_modules/sn-plugin-lib/src/**/*.ts`. Cite both with `path:line`.
    2. Compare against the 0.1.19 baseline by inspecting the relevant hunk in `.planning/research/lib-0.1.19-to-0.1.43-dts.diff`.
    3. Cross-check the SnAlign call site (also cited with `path:line`) — does the existing call still compile and still receive the expected return shape under 0.1.43?
    4. Verdict + Phase 2 action (or "No change").

    Then write the THREE classification sections (§3 / §4 / §5) by walking through the evidence:

    §3 Breaking Changes — every tsc error from `ghost-tsc.log` becomes an entry. Group by file/method when multiple errors share a root cause. Format per entry:
    ```
    ### B-XX: <one-line title>
    - 0.1.19 shape: `<signature or type>` (cite node_modules path:line at 0.1.19 — note that 0.1.34 already resolves in the working tree; use the 0.1.19 install at /tmp/sn-019/... if you need the strict baseline; otherwise cite the .d.ts diff hunk)
    - 0.1.43 shape: `<signature or type>` (cite node_modules path:line)
    - SnAlign impact: <file:line in src/ that breaks>
    - Strict-TS gate (if applicable): names which flag (`noUncheckedIndexedAccess` / `exactOptionalPropertyTypes` / `strict`) fires per D-11
    - Evidence: `lib-0.1.43-ghost-tsc.log` line N
    - Phase 2 action: <specific file:line edit suggestion>
    ```

    §4 Behavioral Changes — same-signature, different-behavior changes. Sourced primarily from jest failures (`ghost-jest.log`) and source-code reads (D-03). For changes types don't surface, tag the entry `paper-only` and explain why the audit can claim it (e.g., a code comment or a removed assertion in `src/`). Each entry MUST include `Phase 2 action:` and (if applicable) a flag tying it to the Unknowns section in Plan 05.

    §5 Net-new APIs — exported symbols present in 0.1.43 that did NOT exist in 0.1.19. Scope per D-06: PluginManager / PluginCommAPI / PluginFileAPI are mandatory; adjacent-domain net-new APIs (anything visible from those three classes' import graph) included; truly unrelated domains (handwriting recognition, layers, custom drawing, sticker subsystem) get a single "Present, not surveyed" line each per D-06. Each Net-new entry MUST include:
    ```
    - Signature: <from .d.ts>
    - Source: <node_modules path:line>
    - Use case (paper read): <one sentence>
    - Phase 3 candidate: yes | no | needs-eval
    ```

    Also include (per D-12): a `### Removed / Deprecated` subsection inside §3 listing APIs that 0.1.19 exposed but 0.1.43 removed or marked deprecated. Each entry tagged `SnAlign-impact: None — not currently called` if applicable, with no Phase 2 action item, but documented for Phase 4's SKILL-01 skill audit.

    The four trailing sections (§6 / §7 / §8) are written as STUBS:
    ```
    ## 6. Four Targeted Answers
    _Filled in Plan 04 (gsd-plan 01-04)._

    ## 7. sn-plugin Skill Gotcha Cross-Reference
    _Filled in Plan 05 (gsd-plan 01-05)._

    ## 8. Unknowns / Paper-only Claims
    _Filled in Plan 05 (gsd-plan 01-05)._
    ```

    Commit on the audit branch:
    `git add .planning/research/lib-0.1.19-to-0.1.43-audit.md`
    `git commit -m "docs(01): audit doc — call-site sweep + Breaking/Behavioral/Net-new buckets"`
  </action>
  <verify>
    <automated>test -s .planning/research/lib-0.1.19-to-0.1.43-audit.md &amp;&amp; grep -c '^## 2\. Call-Site Sweep' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}' &amp;&amp; grep -c '^## 3\. Breaking Changes' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}' &amp;&amp; grep -c '^## 4\. Behavioral Changes' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}' &amp;&amp; grep -c '^## 5\. Net-new APIs' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/lib-0.1.19-to-0.1.43-audit.md` exists and is ≥ 80 lines.
    - Has exactly one `# ` H1 line.
    - Sections `## 2. Call-Site Sweep`, `## 3. Breaking Changes`, `## 4. Behavioral Changes`, `## 5. Net-new APIs` each appear exactly once.
    - Stub sections `## 6. Four Targeted Answers`, `## 7. sn-plugin Skill Gotcha Cross-Reference`, `## 8. Unknowns / Paper-only Claims` each appear exactly once.
    - All 7 SnAlign-touched API names from the bullet list above appear at least once in §2 (grep `-c` each name, ≥ 1): `getLassoRect`, `resizeLassoRect`, `setLassoBoxState`, `getCurrentFilePath`, `getCurrentPageNum`, `closePluginView`, `getPageSize`. (Beyond these 7, the 5 PluginManager methods are also expected.)
    - Every Breaking entry contains the literal string `Phase 2 action:` — verify by counting `### B-` entries and `Phase 2 action:` occurrences; the latter must be ≥ the former.
    - Every Behavioral entry contains `Phase 2 action:` (same count rule applies for `### Beh-` or whichever prefix is used; the doc should describe its own numbering convention near the section header).
    - Every Net-new entry contains `Phase 3 candidate:` (same count rule).
    - At least one entry references `lib-0.1.43-ghost-tsc.log` OR `lib-0.1.43-ghost-jest.log` OR `lib-0.1.19-to-0.1.43-dts.diff` (grep `-c '\(ghost-tsc.log\|ghost-jest.log\|dts.diff\)'` ≥ 1) — proves the doc actually cites evidence.
    - At least one entry cites `node_modules/sn-plugin-lib/src/` OR `lib/typescript/src/` (grep `-c 'node_modules/sn-plugin-lib/\(src\|lib/typescript\)'` ≥ 1) — proves source authority per D-03.
    - Commit landed: `git log --oneline -1` shows the audit-doc commit.
  </acceptance_criteria>
  <done>The audit doc has its full structural skeleton, the call-site sweep is complete (all 7+ rows present with verdicts), and the three classification buckets are populated with entries that each carry a `Phase 2 action:` or `Phase 3 candidate:` line per D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /tmp scratch installs → audit doc claims | Audit cites paths under /tmp for the 0.1.19 baseline; reader must trust those installs match D-04. |
| ghost-tsc/jest logs → Breaking classification | Audit classifies based on those logs; logs are reproducible per D-15. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Repudiation | Audit claim with no evidence citation | mitigate | Acceptance criteria require every Breaking entry to cite `path:line` AND reference at least one evidence file across the doc. |
| T-01-08 | Tampering | Misclassifying a strict-TS-only failure as "not breaking" | mitigate | D-11 + acceptance criteria explicitly require strict-TS failures to be counted as Breaking. |
| T-01-09 | Information Disclosure | Audit doc inadvertently committing absolute paths leaking username | accept | Repo paths only; the few /tmp citations describe the methodology, not embedded secrets. |
</threat_model>

<verification>
- `grep -c 'Phase 2 action:' .planning/research/lib-0.1.19-to-0.1.43-audit.md` ≥ (number of Breaking entries + number of Behavioral entries).
- `grep -c 'Phase 3 candidate:' .planning/research/lib-0.1.19-to-0.1.43-audit.md` ≥ (number of Net-new entries).
- Every API name in the 7-row required call-site list appears at least once.
</verification>

<success_criteria>
- ROADMAP criterion #1: audit doc exists with breaking / behavioral / net-new sections.
- ROADMAP criterion #4: each of the 7 SnAlign call sites is checked and flagged.
- D-10 / D-11 / D-12 / D-13 each have observable artifacts in the doc.
- Stubs for §6 / §7 / §8 are in place for Plans 04+05 to fill.
</success_criteria>

<output>
Create `.planning/phases/01-api-diff-audit/01-03-SUMMARY.md` when done. Record: (a) the per-API verdict for each of the 11 call-site rows, (b) totals for §3 / §4 / §5 (e.g., "Breaking: 3, Behavioral: 1, Net-new: 7"), (c) any entries flagged as `paper-only` so Plan 05's Unknowns section captures them, (d) any "needs-eval" Phase 3 candidates so Plan 04 knows where to direct extra scrutiny.
</output>
