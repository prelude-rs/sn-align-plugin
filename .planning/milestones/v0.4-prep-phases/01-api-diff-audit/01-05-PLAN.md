---
phase: 01-api-diff-audit
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - .planning/research/lib-0.1.19-to-0.1.43-audit.md
autonomous: true
requirements:
  - UPGRADE-05

must_haves:
  truths:
    - "Section 7 of the audit (sn-plugin Skill Gotcha Cross-Reference) lists every existing gotcha from `~/.claude/skills/sn-plugin/SKILL.md` and `references/api-gotchas.md` with a `still valid` / `resolved` / `needs Phase 4 sideload-verify` tag per D-09."
    - "Section 8 (Unknowns / Paper-only Claims) is a numbered list of claims that cannot be verified without on-device sideload, drawn from §3/§4/§6 entries flagged as paper-only or deferred-to-Phase-4, per D-16."
    - "NO files under `~/.claude/skills/sn-plugin/` are modified in this plan — Phase 1 produces the checklist; Phase 4 (SKILL-01..05) does the edits."
  artifacts:
    - path: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      provides: "Sections 7 and 8 populated; §8 ends the document per D-16."
      min_lines: 40
      contains: "## 8. Unknowns / Paper-only Claims"
  key_links:
    - from: ".planning/research/lib-0.1.19-to-0.1.43-audit.md §7"
      to: "~/.claude/skills/sn-plugin/SKILL.md"
      via: "Each gotcha in the skill is mirrored as a row in §7 with a verdict; Phase 4 SKILL-01 consumes the verdicts."
      pattern: "SKILL.md|api-gotchas.md|storage.md"
    - from: ".planning/research/lib-0.1.19-to-0.1.43-audit.md §8"
      to: "Phase 4 (sideload test plan)"
      via: "Phase 4 sideload's test plan is derived from this numbered Unknowns list."
      pattern: "Unknowns"
---

<objective>
Fill in Section 7 (sn-plugin Skill Gotcha Cross-Reference) per D-09 and Section 8 (Unknowns / Paper-only Claims) per D-16. These are the two closing sections of the audit doc.

Purpose: §7 produces the source-of-truth checklist that Phase 4 SKILL-01..05 will read when editing the shared skill files. §8 produces the numbered test-plan checklist that Phase 4's sideload verification will execute on A5X2. Critically — per D-09 — Phase 1 does NOT edit skill files; the cross-reference lives in the audit doc only.

Output: §7 + §8 of `.planning/research/lib-0.1.19-to-0.1.43-audit.md` populated. No edits to any file under `~/.claude/skills/`.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-diff-audit/01-CONTEXT.md
@.planning/research/lib-0.1.19-to-0.1.43-audit.md
@.planning/research/lib-0.1.43-ghost-tsc.log
@.planning/research/lib-0.1.43-ghost-jest.log
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate §7 (skill gotcha cross-reference) per D-09</name>
  <files>.planning/research/lib-0.1.19-to-0.1.43-audit.md</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Net-new API Scope" (D-09 cross-reference rule; Phase 1 does NOT edit skill files)
    - ~/.claude/skills/sn-plugin/SKILL.md §"Common gotchas (verified on A5X2 firmware 2025)" — the numbered list of 43 gotchas. Every entry gets a row in §7.1.
    - ~/.claude/skills/sn-plugin/references/api-gotchas.md — every gotcha entry gets a row in §7.2.
    - ~/.claude/skills/sn-plugin/references/storage.md — every claim relevant to AsyncStorage / KvBackend / node_change gets a row in §7.3.
    - .planning/research/lib-0.1.19-to-0.1.43-audit.md (the already-written §3 / §4 / §5 / §6 — the verdicts in those sections drive the tags in §7).
  </read_first>
  <action>
    Replace the existing stub line `_Filled in Plan 05 (gsd-plan 01-05)._` directly under the `## 7. sn-plugin Skill Gotcha Cross-Reference` heading with the populated table set described below.

    Required structure (heading text must match exactly for downstream grep gates):

    Heading line: `## 7. sn-plugin Skill Gotcha Cross-Reference`

    Then a short preamble paragraph that:
    - States the D-09 rule (Phase 1 does NOT edit skill files; Phase 4 SKILL-01..05 consumes this checklist).
    - Lists the 4 tags being used: `still valid`, `resolved`, `needs Phase 4 sideload-verify`, `new`.

    Then three subsection tables:

    `### 7.1 From SKILL.md "Common gotchas"`
    A table with 4 columns: `#`, `Gotcha (one-line)`, `Tag`, `Evidence / forward reference`. One row per numbered gotcha in `~/.claude/skills/sn-plugin/SKILL.md` (currently 43 entries — count them and verify). The `Tag` column uses one of the 4 tags above. The `Evidence` column either cites a specific entry in §3 / §4 / §5 / §6 OR forwards to §8 (if confirmation needs sideload) OR says "no change observed in 0.1.43 .d.ts diff" with a citation to `lib-0.1.19-to-0.1.43-dts.diff`.

    `### 7.2 From references/api-gotchas.md`
    Same column shape, but first column is the heading/anchor of the entry in `api-gotchas.md` (use the first words of each entry as the row label). One row per distinct gotcha entry.

    `### 7.3 From references/storage.md`
    Same column shape, but first column is the storage claim. Rows MUST include at minimum:
    - "AsyncStorage native module NOT bundled" → tag driven by §6.1 verdict.
    - "node_change/ escape hatch as the persistence path" → tag from §6.1.
    - "In-memory + KvBackend fallback is the recommended pattern" → tag from §6.1.
    Plus any other distinct claims in `references/storage.md`.

    Critical rules:
    - Be exhaustive. Skipping rows defeats D-09's "source-of-truth checklist for Phase 4" purpose.
    - DO NOT edit any file under `~/.claude/skills/`. The acceptance criteria verify this.
    - Where a tag would be `resolved` but Phase 1 only has paper evidence, downgrade to `needs Phase 4 sideload-verify` and add the claim to §8.
    - Where a gotcha is brand-new (discovered during this audit and not previously in any skill file), tag `new` and write the proposed gotcha text in the Evidence column for Phase 4 to lift into SKILL.md.

    Commit:
    `git add .planning/research/lib-0.1.19-to-0.1.43-audit.md`
    `git commit -m "docs(01): audit doc section 7 — sn-plugin skill gotcha cross-reference"`
  </action>
  <verify>
    <automated>bash -c 'set -e; FILE=.planning/research/lib-0.1.19-to-0.1.43-audit.md; test $(grep -c "^### 7\.1" "$FILE") -eq 1; test $(grep -c "^### 7\.2" "$FILE") -eq 1; test $(grep -c "^### 7\.3" "$FILE") -eq 1; ! grep -qF "_Filled in Plan 05 (gsd-plan 01-05)._" "$FILE"'</automated>
  </verify>
  <acceptance_criteria>
    - Heading `## 7. sn-plugin Skill Gotcha Cross-Reference` appears exactly once.
    - Subsections `### 7.1`, `### 7.2`, `### 7.3` each appear exactly once.
    - The stub line `_Filled in Plan 05 (gsd-plan 01-05)._` no longer appears anywhere in the file.
    - At least one of the four tags appears in the audit body: `still valid`, `resolved`, `needs Phase 4 sideload-verify`, `new` (grep `-c` across the alternation ≥ 1; the table is the only place these tags should appear).
    - §7.3 contains the literal string `AsyncStorage` (already there from §6.1, but the row in §7.3 must reference it too).
    - No file under `~/.claude/skills/` is modified during this task — verify by running `git status --porcelain $HOME/.claude/skills/ 2>/dev/null` and confirming empty output (skill files live outside the repo, so `git status` won't see them; this is informational — the acceptance is observational).
    - Commit landed: `git log --oneline -1` references the §7 commit.
  </acceptance_criteria>
  <done>Section 7 is fully populated with three tables (SKILL.md / api-gotchas.md / storage.md) covering every existing gotcha, each tagged. No skill files were edited.</done>
</task>

<task type="auto">
  <name>Task 2: Populate §8 (Unknowns / Paper-only Claims) per D-16</name>
  <files>.planning/research/lib-0.1.19-to-0.1.43-audit.md</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Audit Verification Step" (D-16 closing section format; D-17 audit stands alone as Phase 2/3/4 contract)
    - .planning/research/lib-0.1.19-to-0.1.43-audit.md sections 3, 4, 6, 7 (the prior plans flagged entries as `paper-only` or `needs Phase 4 sideload-verify`; this section consolidates them).
  </read_first>
  <action>
    Replace the stub line `_Filled in Plan 05 (gsd-plan 01-05)._` directly under `## 8. Unknowns / Paper-only Claims` with a populated numbered list.

    Required structure:

    Heading: `## 8. Unknowns / Paper-only Claims`

    Preamble paragraph that:
    - States the D-16 rule: this section is the Phase 4 sideload test plan.
    - States the D-17 invariant: the audit stands alone; this is the final word from Phase 1.

    Then a numbered ordered list. EACH item MUST have:
    - A claim statement (one sentence).
    - A source reference (which §3 / §4 / §6 / §7 entry seeded it).
    - A specific on-device verification step Phase 4 will perform.

    Use this template per entry:
    ```
    1. **<claim>** — Sourced from §<X>. Verify on A5X2 by <specific user action / specific API call sequence>. Expected outcome: <what success looks like>.
    ```

    Items that MUST appear in §8 (cross-check the audit body):
    - The `modifyButtonRes` on-device reliability question (from §6.2; tied to SKILL.md gotcha #2).
    - The AsyncStorage runtime presence question if §6.1 verdict is anything other than a definitive "yes, bundled" or "no, absent". If §6.1 was definitive (the static evidence is unambiguous), still include this item with the claim "AsyncStorage runtime behavior matches static evidence" and verification step "attempt import + getItem/setItem round-trip on device".
    - Any `setLassoBoxState(2)` semantics question that §3 or §4 raised but couldn't fully type-check.
    - Each skill gotcha tagged `needs Phase 4 sideload-verify` in §7 — list every such item.
    - Any new/changed `editDataTypes` value range surfaced by the diff that needs on-device confirmation.

    Items can be added beyond this required set if the audit body surfaced them, but the required ones must be present.

    Final paragraph confirming: "Per D-15, the scratch branch `scratch/ricardo/ghost-0.1.43` is slated for deletion in Plan 06 once this audit doc lands on dev."

    Commit:
    `git add .planning/research/lib-0.1.19-to-0.1.43-audit.md`
    `git commit -m "docs(01): audit doc section 8 — unknowns / paper-only claims (Phase 4 sideload test plan)"`
  </action>
  <verify>
    <automated>bash -c 'set -e; FILE=.planning/research/lib-0.1.19-to-0.1.43-audit.md; test $(grep -c "^## 8\. Unknowns / Paper-only Claims" "$FILE") -eq 1; ! grep -qF "_Filled in Plan 05 (gsd-plan 01-05)._" "$FILE"; test $(grep -c "^[0-9]\+\. " "$FILE") -ge 3'</automated>
  </verify>
  <acceptance_criteria>
    - Heading `## 8. Unknowns / Paper-only Claims` appears exactly once.
    - The stub line `_Filled in Plan 05 (gsd-plan 01-05)._` no longer appears anywhere in the file.
    - At least 3 numbered list items (`^[0-9]+\. ` regex) exist in the file — the realistic minimum given the items required above.
    - §8 mentions `modifyButtonRes` (the §6.2 item is required to land here).
    - §8 mentions `AsyncStorage` (the §6.1 item is required to land here).
    - §8 mentions the scratch branch `scratch/ricardo/ghost-0.1.43` (closing paragraph per D-15 forward reference).
    - §8 is the last section in the file: `tail -50 .planning/research/lib-0.1.19-to-0.1.43-audit.md` shows §8 content, not another `## ` heading after it.
    - Commit landed: `git log --oneline -1` references the §8 commit.
  </acceptance_criteria>
  <done>Section 8 is populated as a numbered list of on-device-verification items, covering at minimum modifyButtonRes, AsyncStorage, and every skill gotcha tagged `needs Phase 4 sideload-verify` in §7. The audit doc now ends at §8 per D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §7 verdicts → Phase 4 skill edits | Phase 4 will lift these tags directly when editing SKILL.md and references/*.md. A wrong tag becomes a wrong skill claim. |
| §8 list → Phase 4 sideload test plan | Every Unknowns item becomes a sideload test case. Missing an item leaves a coverage gap on real hardware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | Editing skill files inside Phase 1 | mitigate | D-09 explicit prohibition; acceptance criteria require the cross-reference to live in the audit doc, not in the skill files. |
| T-01-14 | Repudiation | An §8 item with no source reference | mitigate | Each item template requires "Sourced from §X" pointing back into the audit body. |
| T-01-15 | Information Disclosure | None applicable — docs-only phase. | accept | n/a |
</threat_model>

<verification>
- Audit doc has section 7 with three sub-tables filled.
- Audit doc has section 8 with at least 3 numbered claims; modifyButtonRes and AsyncStorage are each referenced at least once in §8.
- The stub strings introduced in Plan 03 are gone.
- No skill files modified.
</verification>

<success_criteria>
- ROADMAP success criterion #1 strengthened: the audit is genuinely complete (covers Breaking + Behavioral + Net-new + 4 targeted answers + skill cross-reference + Unknowns).
- D-09 satisfied: skill gotcha cross-reference exists in the audit, skill files untouched.
- D-16 satisfied: audit ends with the explicit Unknowns / Paper-only Claims section.
- D-17 satisfied: §8 closes the document as the standalone Phase 2/3/4 contract.
</success_criteria>

<output>
Create `.planning/phases/01-api-diff-audit/01-05-SUMMARY.md` when done. Record: (a) the count of skill gotchas tagged per category (`still valid` / `resolved` / `needs Phase 4 sideload-verify` / `new`), (b) the count of §8 numbered items, (c) any cross-cutting concerns surfaced while writing §7 that warrant a fresh `new` gotcha entry for Phase 4 to add to SKILL.md.
</output>
