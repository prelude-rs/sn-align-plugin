---
phase: 01-api-diff-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/lib-0.1.19-to-0.1.43-dts.diff
autonomous: true
requirements:
  - UPGRADE-05

must_haves:
  truths:
    - "A machine-generated .d.ts diff between sn-plugin-lib@0.1.19 and sn-plugin-lib@0.1.43 exists on disk as an audit-branch evidence artifact — the .d.ts machine-diff augmentation per D-02."
    - "The diff is reproducible from a clean npm cache by re-running the documented commands."
    - "Both endpoints are pinned to exact versions (no caret/range resolution drift) per D-04."
    - "The diff lands at the evidence path mandated by D-05 (`.planning/research/lib-0.1.19-to-0.1.43-dts.diff`) on the audit branch."
  artifacts:
    - path: ".planning/research/lib-0.1.19-to-0.1.43-dts.diff"
      provides: "Raw unified-diff of every .d.ts file under lib/typescript/src/ between 0.1.19 and 0.1.43."
      min_lines: 1
  key_links:
    - from: ".planning/research/lib-0.1.19-to-0.1.43-dts.diff"
      to: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      via: "Plans 03+04 cite this file by relative path when classifying Net-new and Breaking type-surface changes."
      pattern: "lib-0.1.19-to-0.1.43-dts.diff"
---

<objective>
Produce the raw `.d.ts` machine diff between `sn-plugin-lib@0.1.19` and `sn-plugin-lib@0.1.43` (per D-02) and land it in `.planning/research/` on the audit branch.

Purpose: Phase 1 success criterion #1 demands "every breaking change, behavioral change, and net-new API" be cited. The `.d.ts` diff is the coverage backstop — it surfaces type-surface changes (signatures, new exports, removed exports) we would otherwise miss when only inspecting code SnAlign already calls. It feeds Plans 03 (call-site sweep) and 04 (4 targeted answers).

Output: a single committed evidence file at `.planning/research/lib-0.1.19-to-0.1.43-dts.diff`. No audit prose is written in this plan.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-diff-audit/01-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md
@.claude/skills/git/SKILL.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit working branch off dev</name>
  <files>(no files modified yet — branch creation only)</files>
  <read_first>
    - .claude/skills/git/SKILL.md (branch naming + dev-target rules)
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"In scope" (D-15 — no package.json edits on this branch)
  </read_first>
  <action>
    Switch to `dev` and pull the latest:
    `git checkout dev && git pull origin dev`
    Prune squash-merged local branches per the skill (the `for branch in $(git branch …) … gh pr list --state merged …` block). Then create the audit working branch:
    `git checkout -b docs/ricardo/01-api-diff-audit`
    This branch is where ALL `.planning/research/` evidence and the audit doc itself will be committed. Do NOT modify `package.json` on this branch — the ghost upgrade lives on a separate scratch branch (Plan 02).
    Verify with `git branch --show-current` that the current branch is `docs/ricardo/01-api-diff-audit`.
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "docs/ricardo/01-api-diff-audit"</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` outputs `docs/ricardo/01-api-diff-audit`.
    - `git diff dev --stat` is empty (no commits yet on this branch).
    - `package.json` is untouched on this branch (compare to dev: `git diff dev -- package.json` is empty).
  </acceptance_criteria>
  <done>Audit branch created off latest dev, no file modifications yet.</done>
</task>

<task type="auto">
  <name>Task 2: Install 0.1.19 and 0.1.43 into isolated scratch dirs (pinned exact)</name>
  <files>(creates /tmp/sn-019/ and /tmp/sn-043/ outside the repo — no repo files modified)</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Audit Methodology" (D-02 isolated scratch dirs, D-04 strict 0.1.19 baseline — NOT ^0.1.19)
    - node_modules/sn-plugin-lib/package.json (confirm the package layout we'll diff: `lib/typescript/src/**/*.d.ts`)
  </read_first>
  <action>
    Install both versions outside the repo so the working tree's `node_modules` is never touched.

    Set up the baseline (exact 0.1.19, per D-04):
    `mkdir -p /tmp/sn-019 && cd /tmp/sn-019 && npm init -y >/dev/null && npm install --no-save --ignore-scripts sn-plugin-lib@0.1.19`

    Set up the new version (exact 0.1.43):
    `mkdir -p /tmp/sn-043 && cd /tmp/sn-043 && npm init -y >/dev/null && npm install --no-save --ignore-scripts sn-plugin-lib@0.1.43`

    Return to the repo (`cd ${OLDPWD:-/home/ricardo/repo/SN/sn-align-plugin}`).

    Confirm both installs landed the exact pinned version (not a caret-range resolution):
    - `cat /tmp/sn-019/node_modules/sn-plugin-lib/package.json | python3 -c "import sys,json; print(json.load(sys.stdin)['version'])"` MUST print `0.1.19`.
    - Same check against `/tmp/sn-043/...` MUST print `0.1.43`.

    Confirm the `.d.ts` source tree exists in both:
    - `ls /tmp/sn-019/node_modules/sn-plugin-lib/lib/typescript/src/` (non-empty)
    - `ls /tmp/sn-043/node_modules/sn-plugin-lib/lib/typescript/src/` (non-empty)

    If any check fails, abort and surface the failure — the diff is meaningless on the wrong versions (D-04 fidelity invariant).
  </action>
  <verify>
    <automated>test "$(python3 -c "import json; print(json.load(open('/tmp/sn-019/node_modules/sn-plugin-lib/package.json'))['version'])")" = "0.1.19" &amp;&amp; test "$(python3 -c "import json; print(json.load(open('/tmp/sn-043/node_modules/sn-plugin-lib/package.json'))['version'])")" = "0.1.43" &amp;&amp; test -d /tmp/sn-019/node_modules/sn-plugin-lib/lib/typescript/src &amp;&amp; test -d /tmp/sn-043/node_modules/sn-plugin-lib/lib/typescript/src</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/sn-019/node_modules/sn-plugin-lib/package.json` `version` field is exactly `0.1.19`.
    - `/tmp/sn-043/node_modules/sn-plugin-lib/package.json` `version` field is exactly `0.1.43`.
    - Both `/tmp/sn-{019,043}/node_modules/sn-plugin-lib/lib/typescript/src/` directories exist and are non-empty.
    - Working-tree `node_modules/sn-plugin-lib/package.json` is unchanged (verify: `git status -- node_modules` shows nothing, and `git diff -- node_modules/sn-plugin-lib/package.json` is empty — `node_modules` is gitignored but the file itself should still be at the same resolved version).
  </acceptance_criteria>
  <done>Two pinned scratch installs exist outside the repo, ready to be diffed in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the .d.ts machine diff and commit to audit branch</name>
  <files>.planning/research/lib-0.1.19-to-0.1.43-dts.diff</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Audit Methodology" (D-05 evidence path, D-02 diff scope)
    - /tmp/sn-019/node_modules/sn-plugin-lib/lib/typescript/src (one representative file to confirm format)
    - /tmp/sn-043/node_modules/sn-plugin-lib/lib/typescript/src (same)
  </read_first>
  <action>
    Ensure `.planning/research/` exists: `mkdir -p .planning/research`.

    Produce the unified recursive diff over only the `.d.ts` trees so noise (sourcemaps, README, package.json, JS output) doesn't pollute the diff:
    `diff -ruN /tmp/sn-019/node_modules/sn-plugin-lib/lib/typescript/src /tmp/sn-043/node_modules/sn-plugin-lib/lib/typescript/src > .planning/research/lib-0.1.19-to-0.1.43-dts.diff || true`

    Note the `|| true` — `diff` returns exit code 1 when the trees differ (the expected case here); we explicitly accept it. Verify the file is non-empty AND contains at least one `+++ ` and one `--- ` header (proves real unified-diff content, not empty or error text).

    Sanity-check: scan the diff for at least one known-touched API surface (e.g. `PluginCommAPI`, `PluginManager`, `PluginFileAPI`, `getLassoRect`, `setLassoBoxState`, or `Element`). If none are present, the diff likely captured nothing useful — re-check the path arguments and re-run.

    Commit on the audit branch (`docs/ricardo/01-api-diff-audit`):
    `git add .planning/research/lib-0.1.19-to-0.1.43-dts.diff`
    `git commit -m "docs(01): capture .d.ts machine diff for sn-plugin-lib 0.1.19→0.1.43"`

    Do NOT push yet — the rest of the phase commits land first.
  </action>
  <verify>
    <automated>test -s .planning/research/lib-0.1.19-to-0.1.43-dts.diff &amp;&amp; grep -c '^+++ ' .planning/research/lib-0.1.19-to-0.1.43-dts.diff | awk '{exit ($1 &gt;= 1 ? 0 : 1)}' &amp;&amp; grep -c '^--- ' .planning/research/lib-0.1.19-to-0.1.43-dts.diff | awk '{exit ($1 &gt;= 1 ? 0 : 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/lib-0.1.19-to-0.1.43-dts.diff` exists and is non-empty.
    - File contains at least one `+++ ` line and at least one `--- ` line (proof of unified-diff structure).
    - File mentions at least one of `PluginCommAPI`, `PluginManager`, `PluginFileAPI`, `getLassoRect`, `setLassoBoxState`, or `Element` (grep `-c` ≥ 1 across the alternation).
    - `git log --oneline -1` on `docs/ricardo/01-api-diff-audit` shows a `docs(01):` commit touching only `.planning/research/lib-0.1.19-to-0.1.43-dts.diff`.
    - The working-tree `node_modules/sn-plugin-lib` is unchanged.
  </acceptance_criteria>
  <done>The `.d.ts` machine diff exists at the expected path, is well-formed unified-diff output mentioning at least one of the SnAlign-touched API surfaces, and is committed on the audit branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → /tmp scratch dirs | Untrusted package fetch; isolated from project node_modules. |
| /tmp scratch → audit branch | Only the textual diff crosses; no executable/binary content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install of sn-plugin-lib@0.1.19/0.1.43 into /tmp | accept | Packages are the same publisher we already depend on; install uses `--ignore-scripts` to neutralise lifecycle scripts; isolated scratch dirs are outside the repo working tree. |
| T-01-02 | Information Disclosure | Accidentally committing /tmp install artifacts to the audit branch | mitigate | Only `.planning/research/lib-0.1.19-to-0.1.43-dts.diff` is staged in Task 3; the `git commit` invocation names the exact file. |
| T-01-03 | Tampering | Wrong version resolved (caret-drift to 0.1.34) | mitigate | Task 2 verifies `package.json.version` is exactly `0.1.19` / `0.1.43` before Task 3 runs (D-04 fidelity). |
</threat_model>

<verification>
- `git log --oneline -1 docs/ricardo/01-api-diff-audit` references the .d.ts diff commit.
- `git diff dev..docs/ricardo/01-api-diff-audit --stat` shows exactly one new file: `.planning/research/lib-0.1.19-to-0.1.43-dts.diff`.
- The diff file format is valid unified diff (grep gates above).
</verification>

<success_criteria>
- Audit branch `docs/ricardo/01-api-diff-audit` exists off `dev`.
- `.planning/research/lib-0.1.19-to-0.1.43-dts.diff` is created, committed, non-empty, well-formed.
- The repo's working `node_modules` is untouched; `package.json` on this branch is identical to `dev`.
</success_criteria>

<output>
Create `.planning/phases/01-api-diff-audit/01-01-SUMMARY.md` when done. Record: the audit branch name, the diff file path, line count of the diff, and the highest-level summary of which SDK domains the diff touches (e.g., "PluginCommAPI: X changed files; PluginFileAPI: Y; PluginManager: Z; types/*: W") to give Plans 03+04 a map of what to read first.
</output>
