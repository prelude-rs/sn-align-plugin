---
phase: 01-api-diff-audit
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - .planning/research/lib-0.1.19-to-0.1.43-audit.md
autonomous: true
requirements:
  - UPGRADE-05

must_haves:
  truths:
    - "Section 6 of the audit doc (Four Targeted Answers) explicitly answers the AsyncStorage question (D-07): is the native module bundled, is there a new KV API, and what static evidence supports the answer."
    - "Section 6 explicitly answers the modifyButtonRes question (D-08): is the type declared, is the source implementation present, what's the verdict, and what's deferred to Phase 4 sideload."
    - "Section 6 explicitly answers the page-bounds query question: did 0.1.43 add a built-in page-bounds / clamp-rect helper?"
    - "Section 6 explicitly answers the new lasso/page APIs question: did 0.1.43 add any new methods relevant to the SnAlign lasso flow?"
    - "EACH of §6.1, §6.2, §6.3, §6.4 contains at least one `node_modules/sn-plugin-lib/` citation (types AND/OR source) per D-03 authority order — gated per-subsection by the verify automation."
    - "On-device-only claims are flagged for deferral to Phase 4 sideload per D-07 / D-08, with a forward reference to the Unknowns section (Plan 05)."
  artifacts:
    - path: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      provides: "Section 6 expanded with the four ADOPT-01 / ADOPT-02 input answers, each with per-subsection node_modules/sn-plugin-lib/ citations."
      min_lines: 30
      contains: "AsyncStorage"
  key_links:
    - from: ".planning/research/lib-0.1.19-to-0.1.43-audit.md §6"
      to: ".planning/REQUIREMENTS.md (ADOPT-01, ADOPT-02)"
      via: "Phase 3 will read §6 to make the ADOPT-01 (persistent storage) and ADOPT-02 (simplification) decisions."
      pattern: "ADOPT-0[12]"
---

<objective>
Fill in Section 6 of the audit ("Four Targeted Answers") — the ADOPT-01 + ADOPT-02 decision inputs that ROADMAP success criteria #2 and #3 demand. Each answer is a paper analysis backed by `.d.ts` + source citations per D-03; on-device runtime proof is explicitly deferred to Phase 4 per D-07 / D-08. EACH of the four subsections must independently cite at least one path under `node_modules/sn-plugin-lib/` — the verify automation enforces this per-subsection.

Purpose: ROADMAP success criteria #2 (AsyncStorage) and #3 (modifyButtonRes / page-bounds / lasso-page APIs) land in this plan. Phase 3's planner consumes §6 to decide whether ADOPT-01 (swap in-memory storage for persistent KV) and ADOPT-02 (simplify with new APIs) are actually viable.

Output: Section 6 of `.planning/research/lib-0.1.19-to-0.1.43-audit.md` populated with four sub-sections (one per question), each answered with evidence AND each carrying at least one `node_modules/sn-plugin-lib/` citation locally inside its own subsection.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-diff-audit/01-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
@.planning/research/lib-0.1.19-to-0.1.43-dts.diff
@.planning/research/lib-0.1.43-ghost-tsc.log
@.planning/research/lib-0.1.43-ghost-jest.log
@.planning/research/lib-0.1.19-to-0.1.43-audit.md
@src/storage/anchorStorage.ts
@src/buttons/buttonCommon.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Answer the AsyncStorage question (D-07) and the modifyButtonRes question (D-08), with per-subsection citation gating</name>
  <files>.planning/research/lib-0.1.19-to-0.1.43-audit.md</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Net-new API Scope" (D-07 static-only AsyncStorage answer; D-08 type+source check for modifyButtonRes)
    - ~/.claude/skills/sn-plugin/references/storage.md (current skill claim: AsyncStorage native module is NOT bundled; node_change/ escape hatch; KvBackend fallback)
    - ~/.claude/skills/sn-plugin/references/api-gotchas.md (current skill claim: modifyButtonRes declared in .d.ts but firmware-unreliable per gotcha #2)
    - node_modules/sn-plugin-lib/android/ (grep for `AsyncStorage`, `AsyncStoragePackage`, `@react-native-async-storage`, `AsyncLocalStorage` — bundling signals)
    - node_modules/sn-plugin-lib/RtnSupernotePluginCore.podspec (iOS bundling check; expected empty for our purposes)
    - node_modules/sn-plugin-lib/package.json (check `dependencies` / `peerDependencies` for any KV-related entry)
    - node_modules/sn-plugin-lib/src/PluginManager.ts (or equivalent — grep for KV method names: `getItem`, `setItem`, `removeItem`, `set`, `get`, `kvStore`, `storage`)
    - node_modules/sn-plugin-lib/src/NativePluginManager.ts (same)
    - node_modules/sn-plugin-lib/src/PluginFileAPI.ts (same — D-07 lists this surface as a possible host for new KV)
    - node_modules/sn-plugin-lib/lib/typescript/src/PluginManager.d.ts (and the matching .d.ts files for the above) — confirm any KV signature
    - node_modules/sn-plugin-lib/src/PluginManager.ts (search for `modifyButtonRes` implementation — D-08)
    - node_modules/sn-plugin-lib/lib/typescript/src/PluginManager.d.ts (search for `modifyButtonRes` declaration)
    - src/storage/anchorStorage.ts (read the existing KvBackend interface so the AsyncStorage answer can describe what a swap would look like)
    - src/buttons/buttonCommon.ts (SnAlign's optional modifyButtonRes type stub at PluginManagerLike)
  </read_first>
  <action>
    Append to `.planning/research/lib-0.1.19-to-0.1.43-audit.md` under the existing `## 6. Four Targeted Answers` stub. Replace the stub line `_Filled in Plan 04 (gsd-plan 01-04)._` with the populated section.

    **Per-subsection citation requirement (load-bearing):** EACH of §6.1, §6.2, §6.3, §6.4 must contain at least one path that begins with `node_modules/sn-plugin-lib/`. A subsection without a local citation fails the verify gate — even if other subsections cite the same path. Cite types (`.d.ts`) AND/OR source (`src/**/*.ts`) per D-03 authority order; both is preferred.

    Use this layout (headings exact for downstream grep gates):

    ```
    ## 6. Four Targeted Answers

    These answers feed the ADOPT-01 (persistent storage) and ADOPT-02 (simplification) decisions
    in Phase 3. Per D-07/D-08, claims here are static analysis only — on-device runtime
    proof is deferred to Phase 4 sideload (and surfaced in §8 Unknowns).

    Every subsection below cites at least one `node_modules/sn-plugin-lib/...` path locally,
    enforced by the §6 verify gate (per-subsection awk/grep block — see Plan 01-04 Task 1).

    ### 6.1 AsyncStorage — bundled? new KV API?

    Static evidence (each bullet MUST cite a path under `node_modules/sn-plugin-lib/`):
    - native (Android): grep results inside `node_modules/sn-plugin-lib/android/` for `AsyncStorage` / `AsyncStoragePackage` / `@react-native-async-storage`. (Cite each hit path:line, or "no matches" with the grep command shown.)
    - native (iOS): inspection of `node_modules/sn-plugin-lib/RtnSupernotePluginCore.podspec`.
    - peer deps: `node_modules/sn-plugin-lib/package.json` — any AsyncStorage-related dependency or peerDependency?
    - net-new KV surface: walk `node_modules/sn-plugin-lib/src/PluginManager.ts` / `NativePluginManager.ts` / `PluginFileAPI.ts` AND their `lib/typescript/src/**.d.ts` counterparts for KV-shaped methods (`getItem`/`setItem`/`removeItem` or `kvStore` namespaces).

    Verdict:
    - Bundling: <yes | no | partial — with reasoning>
    - New KV API on PluginManager/NativePluginManager/PluginFileAPI: <yes | no — cite the method or "none found">
    - SnAlign-impact (per ADOPT-01): <"swap createMemoryAnchorStorage for the new backend via existing KvBackend interface at src/storage/anchorStorage.ts" if bundling/API found; else "in-memory remains; ADOPT-01 stays deferred — match current skill claim at references/storage.md">
    - Deferred to Phase 4 sideload: <which specific runtime claim remains unverified — feeds §8 Unknowns>

    ### 6.2 modifyButtonRes — does it work in 0.1.43?

    Static evidence (each bullet MUST cite a path under `node_modules/sn-plugin-lib/`):
    - Type declaration: cite the `node_modules/sn-plugin-lib/lib/typescript/src/PluginManager.d.ts` line where `modifyButtonRes` is declared (or note its absence with the file path).
    - Source implementation: cite the `node_modules/sn-plugin-lib/src/PluginManager.ts` line that implements (or stubs / throws) `modifyButtonRes`.
    - Any commit comments, source comments, or README references in the tarball about reliability or firmware support — cite the file in node_modules.
    - Cross-reference: the current `sn-plugin` skill gotcha #2 says "declared in .d.ts but NOT exposed by firmware". Does the 0.1.43 source corroborate / refute / change this?

    Verdict:
    - Type present: <yes | no>
    - Source implementation present: <yes — describes what it does | no — stub | no — removed>
    - Paper assessment: <"may work" | "still unreliable" | "removed">
    - SnAlign-impact (per ADOPT-02): <"adopt with caution after Phase 4 confirms on-device behavior" or "skip — still marked unreliable">
    - Deferred to Phase 4 sideload (per D-08): on-device reliability across A5X2 firmware variants; flag for SKILL-01 / SKILL-02 gotcha audit in Phase 4.

    ### 6.3 Page-bounds query — is there a built-in?

    Static evidence (each bullet MUST cite a path under `node_modules/sn-plugin-lib/`):
    - Walk the 0.1.43 type surface (`node_modules/sn-plugin-lib/lib/typescript/src/PluginCommAPI.d.ts`, `PluginFileAPI.d.ts`) for any new method that returns the page rectangle, page bounds, or page extent (alternatives to the current 3-step `getCurrentFilePath` + `getCurrentPageNum` + `getPageSize` resolver in src/sdk/pageSize.ts). Search terms: `pageBounds`, `pageRect`, `getPageRect`, `clampToPage`, `pageExtent`, `pageInfo`.
    - Walk `node_modules/sn-plugin-lib/src/PluginCommAPI.ts` / `PluginFileAPI.ts` for any `clamp` / `inPage` / `wouldExit` helper.

    Verdict:
    - New page-bounds query: <method name + signature with `node_modules/sn-plugin-lib/...` source citation, or "none found — searched node_modules/sn-plugin-lib/{src,lib/typescript/src}/">
    - SnAlign-impact (per ADOPT-02): <"simplify resolvePageSize" specifying the file/line, or "no win — keep current 3-step + fallback">

    ### 6.4 Other new lasso/page APIs

    Static evidence (each bullet MUST cite a path under `node_modules/sn-plugin-lib/`):
    - Walk the 0.1.43 `node_modules/sn-plugin-lib/lib/typescript/src/PluginCommAPI.d.ts` type surface for net-new methods. Per D-06, only "adjacent-domain" net-new in PluginCommAPI / PluginFileAPI counts; unrelated domains (handwriting recognition, layers, custom drawing) get a "present, not surveyed" mention.
    - Cross-reference with §5 (Net-new APIs) — if §5 already enumerates these, restate the relevant `node_modules/sn-plugin-lib/...` citations inline here so this subsection independently satisfies the per-subsection citation gate. Then add the SnAlign-relevance verdict.

    Verdict per net-new method (concise table or bullet list):
    - <method> (cited at `node_modules/sn-plugin-lib/...`) — Phase 3 candidate: <yes/no/needs-eval> — reason.

    ---
    ```

    Then commit:
    `git add .planning/research/lib-0.1.19-to-0.1.43-audit.md`
    `git commit -m "docs(01): audit doc §6 — four targeted answers (AsyncStorage, modifyButtonRes, page-bounds, lasso/page APIs)"`
  </action>
  <verify>
    <automated>grep -c 'AsyncStorage' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 &gt;= 1 ? 0 : 1)}' &amp;&amp; grep -c 'modifyButtonRes' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 &gt;= 1 ? 0 : 1)}' &amp;&amp; grep -c '^### 6\.1' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}' &amp;&amp; grep -c '^### 6\.2' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}' &amp;&amp; grep -c '^### 6\.3' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}' &amp;&amp; grep -c '^### 6\.4' .planning/research/lib-0.1.19-to-0.1.43-audit.md | awk '{exit ($1 == 1 ? 0 : 1)}' &amp;&amp; for SECTION in "6.1" "6.2" "6.3" "6.4"; do awk "/^### ${SECTION} / {found=1; next} /^### / &amp;&amp; found {exit} found" .planning/research/lib-0.1.19-to-0.1.43-audit.md | grep -q "node_modules/sn-plugin-lib/" || { echo "Missing node_modules/sn-plugin-lib/ citation in §${SECTION}"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - The audit file contains the literal string `AsyncStorage` (grep `-c` ≥ 1).
    - The audit file contains the literal string `modifyButtonRes` (grep `-c` ≥ 1).
    - §6 has four subsections present exactly once each: `### 6.1`, `### 6.2`, `### 6.3`, `### 6.4`.
    - Each subsection has a `Verdict:` line OR contains a clear conclusion under a "Verdict" heading.
    - **Per-subsection citation gate (load-bearing):** EACH of §6.1, §6.2, §6.3, §6.4 contains at least one path under `node_modules/sn-plugin-lib/`. The verify automation enforces this via the `awk` block that extracts each subsection (from `### 6.X ` to the next `### `) and greps for `node_modules/sn-plugin-lib/`. A subsection without a local citation fails the gate.
    - Weak safety net (preserved): the audit file overall contains at least one `node_modules/sn-plugin-lib/` citation (trivially satisfied if the per-subsection gate passes).
    - §6.1 mentions either ADOPT-01 explicitly or forward-references the storage decision in Phase 3.
    - §6.2 contains the literal `deferred to Phase 4` (or equivalent) — wording flexible but must signal Phase 4 sideload deferral per D-08.
    - The stub line `_Filled in Plan 04 (gsd-plan 01-04)._` is no longer present (was replaced).
    - Commit landed: `git log --oneline -1` shows the §6 commit.
  </acceptance_criteria>
  <done>Section 6 of the audit doc is fully populated with the four targeted answers, each backed by at least one `node_modules/sn-plugin-lib/` citation locally inside its own subsection, and each with a clear Phase-3 or Phase-4 forwarding statement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static evidence claim → ADOPT-01/02 decision | Phase 3 commits to engineering work based on §6; wrong claim = wasted effort. |
| Paper-only modifyButtonRes verdict → SKILL-02 update | Phase 4 will edit skill files based on §6.2; misclassification propagates to future Supernote plugins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Repudiation | An §6 subsection with no local citation | mitigate | Verify automation runs a per-subsection `awk` + `grep` gate; EACH of §6.1, §6.2, §6.3, §6.4 must cite `node_modules/sn-plugin-lib/` locally. |
| T-01-11 | Spoofing | Conflating "type present" with "implementation works" for modifyButtonRes | mitigate | D-08 + the §6.2 template explicitly separates type / source / on-device deferral. |
| T-01-12 | Tampering | Falsely declaring AsyncStorage bundled without seeing the native side evidence | mitigate | §6.1 evidence checklist names the four static sources (android/, podspec, package.json, KV-shaped types) — all must be inspected; the per-subsection citation gate forces at least one to be cited inline in §6.1. |
</threat_model>

<verification>
- §6 of the audit doc spans at least 30 lines.
- §6.1 / §6.2 / §6.3 / §6.4 each present exactly once.
- The two literal target tokens (`AsyncStorage`, `modifyButtonRes`) appear at least once each.
- Per-subsection citation gate passes: EACH of §6.1, §6.2, §6.3, §6.4 contains at least one `node_modules/sn-plugin-lib/` path.
</verification>

<success_criteria>
- ROADMAP criterion #2 (AsyncStorage answer) satisfied.
- ROADMAP criterion #3 (modifyButtonRes / page-bounds / lasso-page APIs answer) satisfied.
- §6 directly references the source files cited per D-03 authority order, with per-subsection citation density enforced by the verify automation.
- Anything unverifiable without sideload is flagged for §8 (filled by Plan 05).
</success_criteria>

<output>
Create `.planning/phases/01-api-diff-audit/01-04-SUMMARY.md` when done. Record: (a) verdict for each of the four targeted questions in one line each, (b) the list of claims §6 marks as "deferred to Phase 4 sideload" (feeds the §8 Unknowns section in Plan 05), (c) any skill-file callouts that should land in Phase 4's SKILL-01..05 scope but are NOT to be edited in Phase 1.
</output>
