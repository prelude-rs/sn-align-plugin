---
phase: 01-api-diff-audit
plan: 06
type: execute
wave: 5
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - .planning/research/lib-0.1.19-to-0.1.43-audit.md
autonomous: true
requirements:
  - UPGRADE-05

must_haves:
  truths:
    - "ROADMAP success criterion #1: the audit doc exists with breaking / behavioral / net-new sections all populated and cited."
    - "ROADMAP success criterion #2: §6.1 answers the AsyncStorage question (bundled / new KV API)."
    - "ROADMAP success criterion #3: §6.2 / §6.3 / §6.4 answer modifyButtonRes / page-bounds query / new lasso/page APIs."
    - "ROADMAP success criterion #4: every SnAlign call site is checked in §2 (Call-Site Sweep)."
    - "Every Breaking entry contains a `Phase 2 action:` line (D-13)."
    - "Every Net-new entry contains a `Phase 3 candidate:` flag (D-13)."
    - "The audit ends with `## 8. Unknowns / Paper-only Claims` (D-16)."
    - "The scratch branch `scratch/ricardo/ghost-0.1.43` is deleted locally per D-15; evidence files survive on the audit branch."
    - "All evidence appendix files exist: `.planning/research/lib-0.1.19-to-0.1.43-dts.diff`, `lib-0.1.43-ghost-tsc.log`, `lib-0.1.43-ghost-jest.log`."
    - "D-14 'Done' bar is satisfied: audit doc exists, ghost-tsc and ghost-jest logs are committed appendix files, and every Breaking entry traces back to a tsc error or test failure (or is explicitly tagged `paper-only` with a source-code citation)."
  artifacts:
    - path: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      provides: "Final, verified audit doc with table of contents complete."
      min_lines: 100
      contains: "## 8. Unknowns / Paper-only Claims"
    - path: ".planning/research/lib-0.1.19-to-0.1.43-dts.diff"
      provides: "Evidence appendix — must still exist after this plan."
      min_lines: 1
    - path: ".planning/research/lib-0.1.43-ghost-tsc.log"
      provides: "Evidence appendix — must still exist after this plan."
      min_lines: 1
    - path: ".planning/research/lib-0.1.43-ghost-jest.log"
      provides: "Evidence appendix — must still exist after this plan."
      min_lines: 1
  key_links:
    - from: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      to: "Phase 2 planning (consumes audit per D-17)"
      via: "Phase 2's plan-phase reads the audit and turns `Phase 2 action:` lines into concrete tasks."
      pattern: "Phase 2 action:"
    - from: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      to: "Phase 3 planning (consumes audit per D-17)"
      via: "Phase 3's plan-phase reads §5 / §6 and turns `Phase 3 candidate: yes` flags into adoption tasks."
      pattern: "Phase 3 candidate:"
---

<objective>
Final pass: verify every D-XX decision and every ROADMAP success criterion is observable in the audit doc, then delete the throwaway scratch branch per D-15.

Purpose: D-14's "Done" bar must be satisfiable by inspection — audit + ghost-tsc + ghost-jest evidence, every Breaking entry traceable to a tsc error or test failure (or `paper-only`-tagged), the closing Unknowns section in place. D-17's verifier-can-trust-the-audit invariant rests on this plan landing the final consistency pass.

Output:
- A consistency-pass commit on the audit doc (table-of-contents updated to reflect final section counts; executive summary tightened; any final cross-references fixed).
- The scratch branch `scratch/ricardo/ghost-0.1.43` deleted locally per D-15.
- The audit branch `docs/ricardo/01-api-diff-audit` is ready to be pushed and opened as a PR to `dev`.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-diff-audit/01-CONTEXT.md
@.planning/research/lib-0.1.19-to-0.1.43-audit.md
@.planning/research/lib-0.1.19-to-0.1.43-dts.diff
@.planning/research/lib-0.1.43-ghost-tsc.log
@.planning/research/lib-0.1.43-ghost-jest.log
@.claude/skills/git/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP + D-XX consistency pass and final commit</name>
  <files>.planning/research/lib-0.1.19-to-0.1.43-audit.md</files>
  <read_first>
    - .planning/research/lib-0.1.19-to-0.1.43-audit.md (the full doc — read it end-to-end before editing)
    - .planning/ROADMAP.md (read criteria #1..#4 again, verbatim, and cross-check each against the doc)
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md (D-10 / D-11 / D-12 / D-13 / D-14 / D-16 / D-17)
  </read_first>
  <action>
    Walk the audit doc top-to-bottom and run the following consistency pass. Fix anything that fails; do not commit until all checks pass.

    1. **Table of Contents accuracy.** Open §1's Table of Contents. Confirm it lists §1..§8 with the section titles that actually appear later in the doc. If any title drifted, fix the TOC.

    2. **Executive Summary tightening.** §1 should be one short paragraph that answers: how many entries in each bucket (§3/§4/§5), how many SnAlign call sites are affected, what's the headline ADOPT-01 / ADOPT-02 verdict from §6, how many items are deferred to Phase 4 sideload. Tighten or rewrite this paragraph now that §3..§8 are stable.

    3. **D-13 rule re-check.** Count `Phase 2 action:` occurrences and compare against the count of distinct Breaking + Behavioral entries (use whatever entry-numbering convention the doc uses — e.g. `### B-` and `### Beh-`). Every distinct entry must have at least one `Phase 2 action:` line. Same check for Net-new entries vs `Phase 3 candidate:` lines. Patch any entry missing the required line.

    4. **D-11 rule re-check.** For every Breaking entry that originated from a strict-TS-only failure (clean under stock TS, fails under our strict flags), confirm the entry explicitly names the flag (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `strict`, or `noImplicitOverride`). Patch any entry missing the flag callout.

    5. **D-12 rule re-check.** If §3 has a "Removed / Deprecated" subsection (per Plan 03's structure), confirm every entry there carries either a `Phase 2 action:` line (if SnAlign uses the removed API) OR a `SnAlign-impact: None — not currently called` line (if not). If neither is present on any entry, patch.

    6. **D-14 evidence backstop.** For every Breaking entry, confirm it cites at least one of: a line in `lib-0.1.43-ghost-tsc.log`, a failing test in `lib-0.1.43-ghost-jest.log`, a hunk in `lib-0.1.19-to-0.1.43-dts.diff`, or is explicitly tagged `paper-only` with a source-code citation. Patch any entry that lacks evidence.

    7. **ROADMAP criterion #4 backstop.** §2 Call-Site Sweep must have a row for each of: `PluginManager.init`, `PluginManager.registerButton`, `PluginManager.registerButtonListener`, `PluginManager.getPluginDirPath`, `PluginManager.closePluginView`, `PluginCommAPI.getLassoRect`, `PluginCommAPI.resizeLassoRect`, `PluginCommAPI.setLassoBoxState`, `PluginCommAPI.getCurrentFilePath`, `PluginCommAPI.getCurrentPageNum`, `PluginFileAPI.getPageSize`. Grep for each name (`-c` ≥ 1). Patch §2 to add any missing row before continuing.

    8. **D-16 closing-section backstop.** Confirm `## 8. Unknowns / Paper-only Claims` is the LAST `## ` heading in the doc. If §6 / §7 stubs leaked through, remove them.

    9. **Doc footer.** Add a final line at the end of the doc: `*Audit finalized: <YYYY-MM-DD>. Evidence appendix: ./lib-0.1.19-to-0.1.43-dts.diff, ./lib-0.1.43-ghost-tsc.log, ./lib-0.1.43-ghost-jest.log.*` (use today's date).

    Once all 9 checks pass, commit:
    `git add .planning/research/lib-0.1.19-to-0.1.43-audit.md`
    `git commit -m "docs(01): audit doc final consistency pass — ROADMAP criteria #1..#4 verified"`
  </action>
  <verify>
    <automated>bash -c 'set -e; FILE=.planning/research/lib-0.1.19-to-0.1.43-audit.md; test -s "$FILE"; for name in getLassoRect resizeLassoRect setLassoBoxState getCurrentFilePath getCurrentPageNum closePluginView getPageSize registerButton registerButtonListener getPluginDirPath; do test $(grep -c "$name" "$FILE") -ge 1 || (echo "missing: $name"; exit 1); done; test $(grep -c "^## 8\. Unknowns / Paper-only Claims" "$FILE") -eq 1; test $(grep -c "^# " "$FILE") -eq 1; test $(grep -c "Phase 2 action:" "$FILE") -ge 1; test $(grep -c "Phase 3 candidate:" "$FILE") -ge 1; LAST_H2=$(grep -n "^## " "$FILE" | tail -1); echo "$LAST_H2" | grep -q "Unknowns / Paper-only Claims"; test -s .planning/research/lib-0.1.19-to-0.1.43-dts.diff; test -s .planning/research/lib-0.1.43-ghost-tsc.log; test -s .planning/research/lib-0.1.43-ghost-jest.log'</automated>
  </verify>
  <acceptance_criteria>
    - All 7 SnAlign API names (`getLassoRect`, `resizeLassoRect`, `setLassoBoxState`, `getCurrentFilePath`, `getCurrentPageNum`, `closePluginView`, `getPageSize`) appear in the audit ≥ 1 time each (ROADMAP criterion #4).
    - All 4 PluginManager methods (`registerButton`, `registerButtonListener`, `getPluginDirPath`, plus init / closePluginView already covered) appear ≥ 1 time each.
    - `Phase 2 action:` count ≥ 1 (and matches the count of Breaking + Behavioral entries; verify by inspection).
    - `Phase 3 candidate:` count ≥ 1 (and matches the count of Net-new entries).
    - The literal strings `AsyncStorage` and `modifyButtonRes` each appear ≥ 1 time (ROADMAP criteria #2 and #3).
    - Exactly one `# ` H1 line (the title).
    - The LAST `## ` heading in the file is `## 8. Unknowns / Paper-only Claims` (D-16).
    - The three evidence files all exist and are non-empty:
      - `.planning/research/lib-0.1.19-to-0.1.43-dts.diff`
      - `.planning/research/lib-0.1.43-ghost-tsc.log`
      - `.planning/research/lib-0.1.43-ghost-jest.log`
    - Commit landed: `git log --oneline -1` references the final consistency-pass commit.
  </acceptance_criteria>
  <done>The audit doc is consistent end-to-end. Every ROADMAP success criterion and every D-XX decision has an observable artifact in the doc. Evidence appendix files all present.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the scratch branch per D-15 and confirm audit branch state</name>
  <files>(branch deletion — no files modified)</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Audit Verification Step" (D-15 scratch deletion; D-17 audit is the standalone contract)
    - .claude/skills/git/SKILL.md (branch-cleanup pattern — local-only deletion since the scratch branch was never pushed)
  </read_first>
  <action>
    Confirm we're on the audit branch:
    `git branch --show-current` MUST output `docs/ricardo/01-api-diff-audit`. If not, switch with `git checkout docs/ricardo/01-api-diff-audit`.

    Verify the scratch branch was never pushed to origin:
    `git ls-remote --heads origin scratch/ricardo/ghost-0.1.43` MUST output nothing (empty result). If the branch IS on origin, abort and surface — D-15 only authorises local deletion; remote deletion needs explicit user OK.

    Delete the local scratch branch:
    `git branch -D scratch/ricardo/ghost-0.1.43`

    The `-D` (force delete) is necessary because the scratch branch is not merged to any tracking branch — it was always intended as throwaway. Evidence files survive because they were committed to the audit branch in Plan 02, not the scratch branch.

    Final state check on the audit branch:
    - `git status --porcelain` must output nothing (clean working tree).
    - `git log --oneline dev..docs/ricardo/01-api-diff-audit` should show 5 commits (one per Plan 01-05 + this plan's consistency-pass commit), give or take depending on how many fixup commits Plan 03/04/05 needed.
    - `git diff dev -- package.json` must be empty (the audit branch never touched package.json — that lived only on the deleted scratch branch).

    Do NOT push the audit branch to origin in this task — pushing + opening the PR is the user's call after they've reviewed the audit locally. Surface a clear "ready to push" message and the suggested PR command:
    `git push -u origin docs/ricardo/01-api-diff-audit`
    `gh pr create --base dev --title "docs(01): API diff audit — sn-plugin-lib 0.1.19 → 0.1.43" --body-file .planning/phases/01-api-diff-audit/01-06-SUMMARY.md`
  </action>
  <verify>
    <automated>bash -c 'set -e; test "$(git branch --show-current)" = "docs/ricardo/01-api-diff-audit"; ! git rev-parse --verify scratch/ricardo/ghost-0.1.43 >/dev/null 2>&1; test -z "$(git status --porcelain)"; test -z "$(git diff dev -- package.json)"'</automated>
  </verify>
  <acceptance_criteria>
    - Current branch is `docs/ricardo/01-api-diff-audit`.
    - Scratch branch `scratch/ricardo/ghost-0.1.43` no longer exists locally (`git rev-parse --verify scratch/ricardo/ghost-0.1.43` returns non-zero).
    - Working tree is clean (`git status --porcelain` empty).
    - Audit branch's `package.json` is identical to dev's (`git diff dev -- package.json` empty) — confirms D-04 / D-15 isolation invariant: no package.json changes survive to the audit branch.
    - `git ls-remote --heads origin scratch/ricardo/ghost-0.1.43` returns empty (scratch was never pushed).
    - The three evidence files still exist:
      - `.planning/research/lib-0.1.19-to-0.1.43-dts.diff`
      - `.planning/research/lib-0.1.43-ghost-tsc.log`
      - `.planning/research/lib-0.1.43-ghost-jest.log`
    - The audit doc still exists at `.planning/research/lib-0.1.19-to-0.1.43-audit.md` and is non-empty.
  </acceptance_criteria>
  <done>Scratch branch deleted (D-15 satisfied), audit branch clean and ready to push. Evidence appendix survives on the audit branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratch branch → local repo | Branch contains a package.json bump that must never reach dev/main. |
| audit branch → dev (future PR) | Only the audit doc + three evidence files cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Tampering | Accidentally deleting the audit branch instead of the scratch branch | mitigate | Task 2's first step asserts the current branch is the audit branch; the delete command names the scratch branch explicitly. |
| T-01-17 | Elevation of Privilege | Scratch branch's package.json bump leaking onto dev via accidental push | mitigate | Task 2 verifies the scratch branch was never pushed to origin (`git ls-remote --heads`); local-only delete cannot create a remote ref. |
| T-01-18 | Repudiation | Closing the phase with a criterion still unsatisfied | mitigate | Task 1's consistency pass enumerates every ROADMAP criterion and every D-XX decision with a grep-verifiable check. |
</threat_model>

<verification>
- All four ROADMAP success criteria are observable in the audit doc via grep gates above.
- Scratch branch deleted; audit branch clean and ready to push.
- Evidence files survive.
</verification>

<success_criteria>
- ROADMAP criteria #1, #2, #3, #4 are all observable in `.planning/research/lib-0.1.19-to-0.1.43-audit.md`.
- D-10 through D-17 each have observable artifacts in the doc.
- Scratch branch deleted locally (D-15).
- Audit branch is clean, package.json untouched, ready for `git push` + PR.
- Evidence appendix (dts.diff + ghost-tsc.log + ghost-jest.log) intact.
</success_criteria>

<output>
Create `.planning/phases/01-api-diff-audit/01-06-SUMMARY.md` when done. This summary is the body of the future PR — make it consumable to a reviewer who hasn't been following along. Include:
- Three-line elevator pitch of the audit (what changed, what's new, what's deferred).
- Totals: N Breaking, M Behavioral, K Net-new entries.
- ADOPT-01 verdict (one line; full reasoning in §6.1).
- ADOPT-02 verdict (one line; full reasoning in §6.2 / §6.3 / §6.4).
- Phase 4 sideload test count (N items in §8).
- Confirmation that the scratch branch was deleted and `dev`/`main` were never touched.
- A `Next:` line saying "Run `/gsd:plan-phase 2` after this PR merges to dev."
</output>
