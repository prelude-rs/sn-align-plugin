---
phase: 01-api-diff-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/lib-0.1.43-ghost-tsc.log
  - .planning/research/lib-0.1.43-ghost-jest.log
autonomous: true
requirements:
  - UPGRADE-05

must_haves:
  truths:
    - "A throwaway scratch branch `scratch/ricardo/ghost-0.1.43` exists off dev with package.json bumped to sn-plugin-lib@^0.1.43."
    - "tsc --noEmit output produced under the bumped lib is captured to .planning/research/lib-0.1.43-ghost-tsc.log on the audit branch."
    - "jest output produced under the bumped lib is captured to .planning/research/lib-0.1.43-ghost-jest.log on the audit branch."
    - "Evidence files live on the audit branch (docs/ricardo/01-api-diff-audit); the scratch branch is the source but is slated for deletion in Plan 06 per D-15."
  artifacts:
    - path: ".planning/research/lib-0.1.43-ghost-tsc.log"
      provides: "Raw stdout+stderr from `npx tsc --noEmit` against sn-plugin-lib@^0.1.43."
      min_lines: 1
    - path: ".planning/research/lib-0.1.43-ghost-jest.log"
      provides: "Raw stdout+stderr from `npx jest` against sn-plugin-lib@^0.1.43."
      min_lines: 1
  key_links:
    - from: ".planning/research/lib-0.1.43-ghost-tsc.log"
      to: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      via: "Plan 03 cites each tsc error by file:line when classifying Breaking changes per D-11."
      pattern: "ghost-tsc.log"
    - from: ".planning/research/lib-0.1.43-ghost-jest.log"
      to: ".planning/research/lib-0.1.19-to-0.1.43-audit.md"
      via: "Plan 03 cites failing test names when documenting behavioral changes per D-10."
      pattern: "ghost-jest.log"
---

<objective>
Run the ghost-upgrade per D-01: bump `sn-plugin-lib` to `^0.1.43` on a throwaway scratch branch, run `npx tsc --noEmit` and `npx jest`, capture both outputs as evidence files on the audit branch.

Purpose: This is the PRIMARY signal source for the audit (per D-01). Strict-TS compile failures expose every type-surface break against the SnAlign call sites; jest failures expose behavioral / mock-shape drift. Together they let Plan 03 cite concrete file:line evidence for every Breaking entry per D-14.

Output: two evidence logs committed on the audit branch. The scratch branch itself is intentionally a throwaway — it's deleted in Plan 06 (per D-15). `dev` and `main` are never touched.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-diff-audit/01-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
@.claude/skills/git/SKILL.md
@CLAUDE.md
@package.json
@tsconfig.json
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scratch branch and bump package.json to ^0.1.43</name>
  <files>(scratch branch only — no files modified on the audit branch in this task)</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Audit Methodology" (D-01 ghost-upgrade; D-15 scratch deletion)
    - package.json (read the current `sn-plugin-lib` line and surrounding deps so the bump is surgical)
    - .claude/skills/git/SKILL.md (branch naming + clean-tree expectation before checkout)
  </read_first>
  <action>
    First confirm the working tree is clean: `git status --porcelain` should be empty. If the Plan 01 commit is pending and uncommitted, stash or commit it before continuing.

    Branch off the latest `dev`, NOT off the audit branch (we want a pristine starting point so the ghost-tsc / ghost-jest output reflects the upgrade and nothing else):
    `git fetch origin dev && git checkout dev && git pull origin dev`
    `git checkout -b scratch/ricardo/ghost-0.1.43`

    Use `npm pkg set` (built-in npm 7+ helper) to bump exactly the one entry — avoids any chance of accidentally reformatting unrelated package.json fields:
    `npm pkg set dependencies.sn-plugin-lib='^0.1.43'`

    Run `npm install --ignore-scripts` to update the lockfile and resolve the new version. The `--ignore-scripts` flag neutralises any postinstall scripts in transitive deps.

    Verify the resolved version is 0.1.43 (not a higher version that may have shipped since): `node -p "require('sn-plugin-lib/package.json').version"` MUST start with `0.1.43`. If npm resolves a newer 0.1.x patch, that's acceptable; if it resolves something outside `0.1.x`, abort.

    Commit the bump on the scratch branch only:
    `git add package.json package-lock.json`
    `git commit -m "chore: ghost-bump sn-plugin-lib to ^0.1.43 (audit scratch — do not merge)"`
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "scratch/ricardo/ghost-0.1.43" &amp;&amp; node -p "require('sn-plugin-lib/package.json').version" | grep -q '^0\.1\.43'</automated>
  </verify>
  <acceptance_criteria>
    - Current branch is `scratch/ricardo/ghost-0.1.43`.
    - `package.json` `dependencies.sn-plugin-lib` reads `^0.1.43` (grep: `grep -F '"sn-plugin-lib": "^0.1.43"' package.json`).
    - `node -p "require('sn-plugin-lib/package.json').version"` prints `0.1.43`.
    - The audit branch (`docs/ricardo/01-api-diff-audit`) is untouched — `git diff docs/ricardo/01-api-diff-audit -- package.json` shows the `^0.1.19 → ^0.1.43` diff (we're on a different branch with a different version).
  </acceptance_criteria>
  <done>Scratch branch exists with the lib bumped to ^0.1.43 and the new version resolved in node_modules.</done>
</task>

<task type="auto">
  <name>Task 2: Capture ghost tsc + jest evidence logs</name>
  <files>(on scratch branch first — final commit lives on audit branch in Task 3)</files>
  <read_first>
    - .planning/codebase/STACK.md §"Strict TypeScript flags" (noUncheckedIndexedAccess, exactOptionalPropertyTypes are load-bearing per D-11)
    - tsconfig.json (confirm strict flags are present)
    - jest.config.js (confirm the test runner config)
    - package.json scripts section (confirm available commands)
  </read_first>
  <action>
    On `scratch/ricardo/ghost-0.1.43`, run the two evidence commands and capture stdout+stderr to logs OUTSIDE `.planning/` (we don't want them committed on the scratch branch). Use `/tmp/` so they survive between bash invocations in this plan but are not in the repo tree.

    `npx tsc --noEmit 2>&1 | tee /tmp/lib-0.1.43-ghost-tsc.log` — let it run to completion regardless of exit code. The log captures both successful and failing typecheck output.

    `npx jest 2>&1 | tee /tmp/lib-0.1.43-ghost-jest.log` — same: capture pass-or-fail output. Jest is configured with the `react-native` preset (per jest.config.js); 7 suites / 87 tests are expected.

    After both commands complete, verify both log files are non-empty (`test -s`). The tsc log is expected to be small if nothing breaks; the jest log includes per-suite summaries and should be at minimum a dozen lines.

    Do NOT commit these logs on the scratch branch — they will be carried to the audit branch in Task 3.
  </action>
  <verify>
    <automated>test -s /tmp/lib-0.1.43-ghost-tsc.log &amp;&amp; test -s /tmp/lib-0.1.43-ghost-jest.log</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/lib-0.1.43-ghost-tsc.log` exists, non-empty.
    - `/tmp/lib-0.1.43-ghost-jest.log` exists, non-empty.
    - `/tmp/lib-0.1.43-ghost-jest.log` contains a `Tests:` summary line (grep: `grep -c '^Tests:' /tmp/lib-0.1.43-ghost-jest.log` ≥ 1) OR clear indication of a hard runner failure (e.g. `SyntaxError`, `Cannot find module`) — either is valid evidence; "ran but produced nothing" is not.
    - `git status --porcelain` on scratch shows no `.log` files staged or tracked.
  </acceptance_criteria>
  <done>Both evidence logs exist in /tmp; their content is the raw record of how the project compiles and tests under the upgraded lib.</done>
</task>

<task type="auto">
  <name>Task 3: Move evidence logs onto audit branch and commit</name>
  <files>.planning/research/lib-0.1.43-ghost-tsc.log, .planning/research/lib-0.1.43-ghost-jest.log</files>
  <read_first>
    - .planning/phases/01-api-diff-audit/01-CONTEXT.md §"Audit Methodology" (D-05 evidence file paths; D-15 evidence survives scratch deletion because it lives on the audit branch)
    - /tmp/lib-0.1.43-ghost-tsc.log (confirm the captured output looks plausible before committing)
    - /tmp/lib-0.1.43-ghost-jest.log (same)
  </read_first>
  <action>
    Switch back to the audit branch (the working tree must be clean on the scratch branch first — Task 1's commit already covers package.json/lockfile, and Task 2 produced /tmp files only):
    `git checkout docs/ricardo/01-api-diff-audit`

    Verify the working tree is back at the audit branch's `^0.1.19` state: `node -p "require('sn-plugin-lib/package.json').version"` will print whatever the audit branch resolves to (likely `0.1.34` from the existing lockfile). That's expected — the audit branch's package.json is unchanged.

    Ensure the research directory exists: `mkdir -p .planning/research`.

    Copy the evidence logs into place:
    `cp /tmp/lib-0.1.43-ghost-tsc.log .planning/research/lib-0.1.43-ghost-tsc.log`
    `cp /tmp/lib-0.1.43-ghost-jest.log .planning/research/lib-0.1.43-ghost-jest.log`

    Stage and commit BOTH together (single commit keeps the evidence atomic):
    `git add .planning/research/lib-0.1.43-ghost-tsc.log .planning/research/lib-0.1.43-ghost-jest.log`
    `git commit -m "docs(01): capture ghost-upgrade tsc + jest evidence for sn-plugin-lib@^0.1.43"`

    Do NOT delete the scratch branch in this task — it stays around so Plans 03/04 can re-run commands on it if they need to clarify a finding. Plan 06 handles deletion per D-15.
  </action>
  <verify>
    <automated>test -s .planning/research/lib-0.1.43-ghost-tsc.log &amp;&amp; test -s .planning/research/lib-0.1.43-ghost-jest.log &amp;&amp; test "$(git branch --show-current)" = "docs/ricardo/01-api-diff-audit"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/lib-0.1.43-ghost-tsc.log` exists on the audit branch, non-empty.
    - `.planning/research/lib-0.1.43-ghost-jest.log` exists on the audit branch, non-empty.
    - `git log --oneline -1 docs/ricardo/01-api-diff-audit` references the evidence-capture commit.
    - `git diff dev..docs/ricardo/01-api-diff-audit --stat` lists both new log files (alongside the dts.diff from Plan 01).
    - The scratch branch `scratch/ricardo/ghost-0.1.43` still exists locally (`git branch | grep -F scratch/ricardo/ghost-0.1.43`) — deletion is deferred to Plan 06.
  </acceptance_criteria>
  <done>Both evidence logs are on the audit branch and committed; the scratch branch survives for follow-up clarifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → scratch branch | Bumped package.json — must NOT round-trip back to dev or main. |
| scratch branch /tmp logs → audit branch | Plain-text evidence files copied verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | npm install of sn-plugin-lib@^0.1.43 transitive deps on scratch | accept | `--ignore-scripts` neutralises postinstall hooks; scratch branch is throwaway (D-15). |
| T-01-05 | Elevation of Privilege | Accidentally pushing scratch branch to origin or merging it to dev | mitigate | The scratch commit message includes "do not merge"; Plan 06 deletes the local scratch branch; no `git push` is issued in this plan. |
| T-01-06 | Information Disclosure | Logs leaking absolute paths / username | accept | Logs contain only project-relative TS/Jest output; the audit doc consuming them only cites file:line references. |
</threat_model>

<verification>
- `git log --oneline docs/ricardo/01-api-diff-audit` shows the .d.ts diff commit (from Plan 01) AND the ghost evidence commit (from this plan).
- `git diff dev..docs/ricardo/01-api-diff-audit -- .planning/research/ --stat` lists three new files.
- The audit branch's `package.json` is byte-identical to `dev`'s `package.json` (`git diff dev -- package.json` empty).
</verification>

<success_criteria>
- `.planning/research/lib-0.1.43-ghost-tsc.log` exists, non-empty, committed.
- `.planning/research/lib-0.1.43-ghost-jest.log` exists, non-empty, committed.
- Scratch branch `scratch/ricardo/ghost-0.1.43` exists locally for follow-up; not pushed to origin.
- Audit branch's package.json/lockfile remain at the dev baseline.
</success_criteria>

<output>
Create `.planning/phases/01-api-diff-audit/01-02-SUMMARY.md` when done. Record: (a) the headline tsc result (clean vs N errors, with rough breakdown of error types if N > 0), (b) the headline jest result (X passed / Y failed across Z suites), (c) the names of any test suites that failed, (d) the scratch branch name (for Plan 06's deletion step). This summary is the orientation map Plans 03+04 use to know whether to expect mostly type-surface drift, mostly behavioral drift, or a clean upgrade.
</output>
