---
phase: 03-adopt-high-value-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/anchorStorage.ts
  - __tests__/anchorStorage.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md
autonomous: true
requirements: [ADOPT-01, ADOPT-02]
tags: [chore, yagni-cleanup, decision-record, adopt-decline]

must_haves:
  truths:
    - "ADOPT-01 decision recorded — in-memory storage remains; KvBackend dead code removed per D-04 (YAGNI: `KvBackend` type + `createKvBackedAnchorStorage` factory deleted); reopener trigger documented (D-01, D-02, D-04, ROADMAP §3 SC#1)."
    - "ADOPT-02 decision recorded — all 5 candidate simplifications declined with audit citations: `modifyButtonRes` (D-06), page-bounds query (D-07, audit §6.3), `lassoElements` (D-08), `generateLassoPreview` (D-09), `showPluginView` (D-10) (ROADMAP §3 SC#2)."
    - "Orthogonal `config` ⟂ `anchorBox` invariant still holds — no adoption coupled previously-independent writes (ROADMAP §3 SC#3)."
    - "All Jest tests still pass (count drops 87 → ~77 from D-05 KvBackend test removal; ROADMAP §3 SC#4)."
    - "Typecheck (tsc --noEmit) remains green under strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes."
    - "Lint (eslint) and format (prettier --check) remain green."
    - "Build (`npm run build`) still produces `build/outputs/SnAlign.snplg`."
    - "Phase 4 planner-handoff present in 03-01-SUMMARY.md — audit §8.2, §8.4, §8.5, §8.6 dropped from Phase 4 scope per D-03, D-08, D-09, D-10, D-11."
    - "Two distinct commits land in order per D-16: refactor first, then docs."
    - "Plan shape honors meta-decisions: D-12 (decision record = CONTEXT + SUMMARY + REQUIREMENTS + PROJECT; no new `.planning/research/ADOPT-decisions.md` doc), D-15 (branch ops `chore/ricardo/03-adopt-decisions` off `dev` are the user/orchestrator's concern, not the plan's, per workflow branch invariant), D-17 (single PLAN.md `03-01-PLAN.md` — no split)."
  artifacts:
    - path: "src/storage/anchorStorage.ts"
      provides: "AnchorStorage interface + memory backend only — KvBackend type and createKvBackedAnchorStorage function removed."
      contains_not: "KvBackend"
      contains_not_alt: "createKvBackedAnchorStorage"
      still_exports: ["AnchorStorage", "AnchorState", "AnchorEnvelope", "DEFAULT_ANCHOR_STATE", "createMemoryAnchorStorage", "getDefaultAnchorStorage", "__resetDefaultAnchorStorageForTest"]
    - path: "__tests__/anchorStorage.test.ts"
      provides: "Memory storage + getDefaultAnchorStorage test coverage only — KvBackend describe block removed."
      contains_not: "createKvBackedAnchorStorage"
      still_contains: ["describe('memory storage'", "describe('getDefaultAnchorStorage'"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "ADOPT-01 and ADOPT-02 flipped [ ] → [x] with verdict notes per D-13 format (audit citation + reopener trigger)."
      pattern: "^- \\[x\\] \\*\\*ADOPT-0[12]\\*\\*"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table reflects Phase 3 outcomes per D-14 — existing ADOPT-01 row Outcome flipped + new ADOPT-02 row added."
      pattern: "✓ Declined"
    - path: ".planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md"
      provides: "Decision record with audit cross-references, test-count change (87 → ~77), and Phase 4 planner handoff note."
      min_lines: 60
  key_links:
    - from: "src/storage/anchorStorage.ts"
      to: "index.js"
      via: "getDefaultAnchorStorage() — the load-bearing entry point; never references KvBackend"
      pattern: "getDefaultAnchorStorage"
    - from: ".planning/REQUIREMENTS.md (ADOPT-01)"
      to: ".planning/research/lib-0.1.19-to-0.1.43-audit.md §6.1"
      via: "verdict note cites audit subsection per D-13"
      pattern: "§6\\.1"
    - from: ".planning/REQUIREMENTS.md (ADOPT-02)"
      to: ".planning/research/lib-0.1.19-to-0.1.43-audit.md §6.2/§6.3/§5/§6.4"
      via: "verdict note cites audit subsections per D-13"
      pattern: "§6\\.[234]|§5"
    - from: ".planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md"
      to: ".planning/research/lib-0.1.19-to-0.1.43-audit.md §6.1, §6.2, §6.3, §6.4"
      via: "decision record cross-references every dispositive audit subsection"
      pattern: "audit §6\\."
---

<objective>
Lock Phase 3 ADOPT-01 and ADOPT-02 verdicts (both Decline — paper-dispositive per Phase 1 audit) through one YAGNI code cleanup (remove dead `KvBackend` scaffolding) and a multi-doc decision record. No on-device work, no source edits beyond `src/storage/anchorStorage.ts` and its test file.

Purpose: ROADMAP Phase 3 Success Criteria #1 and #2 require recording the ADOPT-01/02 verdicts. Audit §6.1 dispositively confirms no AsyncStorage bundling and no new KV API → ADOPT-01 declines. Audit §6.2/§6.3 + §5 N-01/N-03/N-04 + §6.4 dispositively confirm no net-LOC-reducing simplification → ADOPT-02 declines all 5 candidates. With the verdict declined, the future-ready `KvBackend` stub becomes dead code (D-04) and its 10 tests become dead test code (D-05); remove both.

Output: 2 source-tree files mutated (`src/storage/anchorStorage.ts`, `__tests__/anchorStorage.test.ts`), 2 planning docs updated (`.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`), 1 new SUMMARY (`03-01-SUMMARY.md`). Two distinct commits per D-16. Phase 4 planner is told to drop audit §8.2/§8.4/§8.5/§8.6 from its scope.
</objective>

<execution_context>
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/workflows/execute-plan.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ricardo/repo/SN/sn-align-plugin/CLAUDE.md
@/home/ricardo/repo/SN/sn-align-plugin/.claude/skills/git/SKILL.md
@/home/ricardo/repo/SN/sn-align-plugin/.planning/PROJECT.md
@/home/ricardo/repo/SN/sn-align-plugin/.planning/ROADMAP.md
@/home/ricardo/repo/SN/sn-align-plugin/.planning/REQUIREMENTS.md
@/home/ricardo/repo/SN/sn-align-plugin/.planning/STATE.md
@/home/ricardo/repo/SN/sn-align-plugin/.planning/phases/03-adopt-high-value-wins/03-CONTEXT.md
@/home/ricardo/repo/SN/sn-align-plugin/.planning/research/lib-0.1.19-to-0.1.43-audit.md
@/home/ricardo/repo/SN/sn-align-plugin/.planning/phases/02-compatibility-upgrade/02-01-SUMMARY.md
@/home/ricardo/repo/SN/sn-align-plugin/src/storage/anchorStorage.ts
@/home/ricardo/repo/SN/sn-align-plugin/__tests__/anchorStorage.test.ts

<interfaces>
<!-- Public surface of src/storage/anchorStorage.ts that survives this plan. -->
<!-- These exports MUST still be present and importable after the refactor. -->

```typescript
// src/storage/anchorStorage.ts (post-refactor)

export const ANCHOR_STORAGE_KEY: string;            // see <deferred> in CONTEXT.md — executor may drop this in the same commit

export type AnchorState = {
  readonly config: AlignmentConfig;
  readonly anchorBox: Rect | null;
};

export type AnchorEnvelope = {
  readonly version: 3;
  readonly config: AlignmentConfig;
  readonly anchorBox: Rect | null;
};

export const DEFAULT_ANCHOR_STATE: AnchorState;

export interface AnchorStorage {
  load(): Promise<AnchorState>;
  save(state: AnchorState): Promise<void>;
  setConfig(config: AlignmentConfig): Promise<void>;
  setAnchorBox(box: Rect | null): Promise<void>;
}

export const createMemoryAnchorStorage:
  (initial?: AnchorState) => AnchorStorage;

export const getDefaultAnchorStorage: () => AnchorStorage;

export const __resetDefaultAnchorStorageForTest: () => void;
```

<!-- REMOVED — must NOT appear after refactor: -->
<!--   type KvBackend = { getItem, setItem, removeItem }   (lines ~55-59 pre-refactor) -->
<!--   export const createKvBackedAnchorStorage = ...      (lines ~111-129 pre-refactor) -->

<!-- index.js consumes only getDefaultAnchorStorage() — verified by CONTEXT.md §Integration Points. -->
<!-- No other src/ file imports createKvBackedAnchorStorage; verify with: -->
<!--   grep -rn 'createKvBackedAnchorStorage' src/ index.js App.tsx -->
<!--   (expected: zero matches after refactor) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor — remove KvBackend dead code and its tests</name>
  <files>src/storage/anchorStorage.ts, __tests__/anchorStorage.test.ts</files>
  <read_first>
    - `/home/ricardo/repo/SN/sn-align-plugin/src/storage/anchorStorage.ts` (full file — line numbers may have drifted since CONTEXT.md was written; verify the regions before editing)
    - `/home/ricardo/repo/SN/sn-align-plugin/__tests__/anchorStorage.test.ts` (full file — confirm the `describe('KV-backed storage', ...)` block boundary)
    - `/home/ricardo/repo/SN/sn-align-plugin/.planning/phases/03-adopt-high-value-wins/03-CONTEXT.md` §decisions D-04 and D-05 (load-bearing for this task)
    - `/home/ricardo/repo/SN/sn-align-plugin/CLAUDE.md` §"Commands" (the 5 CI gates this task must keep green)
  </read_first>
  <action>
    Perform a focused YAGNI cleanup of `src/storage/anchorStorage.ts` and `__tests__/anchorStorage.test.ts` per D-04 and D-05. Both edits land in ONE commit (per D-16 commit 1).

    Step 1 — Edit `src/storage/anchorStorage.ts`:
    1a. Re-read the file in full. Line numbers in CONTEXT.md (~55-59 for `KvBackend`, ~111-129 for `createKvBackedAnchorStorage`) are pre-refactor approximations; locate the exact regions by their content, not by line.
    1b. Delete the `type KvBackend = { ... }` block. Pre-refactor it reads:
        ```
        type KvBackend = {
          getItem: (key: string) => Promise<string | null>;
          setItem: (key: string, value: string) => Promise<void>;
          removeItem: (key: string) => Promise<void>;
        };
        ```
    1c. Delete the entire `export const createKvBackedAnchorStorage = (backend: KvBackend, logger?: Pick<Logger, 'error'>): AnchorStorage => buildStorage(...)` function (its body spans the read-side `try { ... } catch (e) { ... }` and write-side `try { ... } catch (e) { ... }` closures that reference `ANCHOR_STORAGE_KEY` and `parseEnvelope` / `serialiseEnvelope`).
    1d. Preserve: every other top-level export (`ANCHOR_STORAGE_KEY`, `AnchorState`, `AnchorEnvelope`, `DEFAULT_ANCHOR_STATE`, `AnchorStorage` interface), the `parseEnvelope` / `serialiseEnvelope` schema-v3 helpers, the `buildStorage` internal helper, `createMemoryAnchorStorage`, `getDefaultAnchorStorage`, and `__resetDefaultAnchorStorageForTest`. These are load-bearing for the rest of the codebase.
    1e. Preserve the file-top docstring block. Optionally tighten the docstring to remove the now-stale "createKvBackedAnchorStorage stays exported against any future firmware..." sentence (line ~10-12 pre-refactor) — but if you reword it, keep the "in-memory only" rationale intact. This is a cosmetic discretionary edit; if unsure, leave it untouched (the doc still reads correctly with the function removed).
    1f. `ANCHOR_STORAGE_KEY` discretion (per CONTEXT.md `<deferred>`): the const becomes unused after `createKvBackedAnchorStorage` removal. EXECUTOR DECISION:
        - If straightforward to drop in this same commit (no downstream importers), remove it. Verify nothing imports it:
          `grep -rn 'ANCHOR_STORAGE_KEY' src/ index.js App.tsx __tests__/` — if no matches outside `anchorStorage.ts` itself and the test file's now-deleted block, drop it.
        - The test file imports it (`import { ANCHOR_STORAGE_KEY, ... }` at line 2 of `__tests__/anchorStorage.test.ts` pre-refactor); once the KV-backed describe block is deleted, that import is also unused. If you drop the const, also drop the import.
        - If anything makes this non-trivial, leave the const + import and capture as a follow-up in the SUMMARY's "Deviations from Plan" or "Known Stubs" section. Do not block the commit on this.

    Step 2 — Edit `__tests__/anchorStorage.test.ts`:
    2a. Re-read the file in full. Confirm the `describe('KV-backed storage', () => { ... })` block starts at the line beginning `describe('KV-backed storage'` and ends at its matching closing `})` immediately before `describe('getDefaultAnchorStorage', ...)`.
    2b. Delete the entire `describe('KV-backed storage', ...)` block. Pre-refactor this is the region spanning lines ~75-175 (10 `it(...)` tests).
    2c. Delete the `fakeKv = () => { ... }` helper above the deleted describe block (pre-refactor lines ~28-42 — the helper exists only to support the KV-backed tests and has no consumer in the surviving `memory storage` or `getDefaultAnchorStorage` blocks; verify by searching the post-edit file for `fakeKv` — expected: zero matches).
    2d. Update the import list at the top of the test file (line 1-9 pre-refactor):
        - REMOVE `createKvBackedAnchorStorage` from the import.
        - REMOVE `ANCHOR_STORAGE_KEY` from the import IFF you also dropped the const in Step 1f. Otherwise keep it.
    2e. Preserve: the `describe('memory storage', ...)` block (lines ~44-73 pre-refactor) and the `describe('getDefaultAnchorStorage', ...)` block (lines ~177-190 pre-refactor) byte-for-byte. The `afterEach(() => __resetDefaultAnchorStorageForTest())` line stays. The `sampleConfig` and `sampleState` consts stay (both blocks reference them).

    Step 3 — Stage and commit. After both edits, stage exactly the two files and commit:
        ```
        git add src/storage/anchorStorage.ts __tests__/anchorStorage.test.ts
        git commit -m "refactor(03): remove KvBackend dead code + drop KvBackend tests"
        ```
        Commit message verbatim per D-16. The commit may also include the `ANCHOR_STORAGE_KEY` removal per Step 1f's discretionary outcome — that's fine, it stays within the refactor's intent.

    Step 4 — Run the 5 CI gates locally (per CLAUDE.md §"Commands" and `.claude/skills/git/SKILL.md`). All must exit 0:
        ```
        npx eslint src/ App.tsx index.js __tests__/
        npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts"
        npx tsc --noEmit
        npm test
        npm run build
        ```
        On the jest gate, the expected `Tests:` line shifts from `87 passed, 87 total` (Phase 2 baseline per `02-01-SUMMARY.md`) to a count strictly less than 87 and at least 70 (the surviving memory + getDefaultAnchorStorage blocks have 7 `it(...)` tests; the rest of the suite (~70 across 6 other files) carries the load). Per D-05, "Test count drops 87 → ~77 (final number determined at execute time by re-running npx jest)". Capture the exact post-refactor count for the SUMMARY in Task 2.
  </action>
  <verify>
    <automated>
      cd /home/ricardo/repo/SN/sn-align-plugin && \
      (test "$(grep -c 'KvBackend' src/storage/anchorStorage.ts)" -eq 0 || (echo "FAIL: KvBackend still in anchorStorage.ts"; exit 1)) && \
      (test "$(grep -c 'createKvBackedAnchorStorage' src/storage/anchorStorage.ts)" -eq 0 || (echo "FAIL: createKvBackedAnchorStorage still in anchorStorage.ts"; exit 1)) && \
      (test "$(grep -c 'createKvBackedAnchorStorage' __tests__/anchorStorage.test.ts)" -eq 0 || (echo "FAIL: createKvBackedAnchorStorage still in test"; exit 1)) && \
      (test "$(grep -c 'fakeKv' __tests__/anchorStorage.test.ts)" -eq 0 || (echo "FAIL: fakeKv helper still in test"; exit 1)) && \
      (grep -q 'export const createMemoryAnchorStorage' src/storage/anchorStorage.ts || (echo "FAIL: createMemoryAnchorStorage removed accidentally"; exit 1)) && \
      (grep -q 'export const getDefaultAnchorStorage' src/storage/anchorStorage.ts || (echo "FAIL: getDefaultAnchorStorage removed accidentally"; exit 1)) && \
      (grep -q "describe('memory storage'" __tests__/anchorStorage.test.ts || (echo "FAIL: memory storage describe block removed accidentally"; exit 1)) && \
      (grep -q "describe('getDefaultAnchorStorage'" __tests__/anchorStorage.test.ts || (echo "FAIL: getDefaultAnchorStorage describe block removed accidentally"; exit 1)) && \
      npx eslint src/ App.tsx index.js __tests__/ && \
      npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts" && \
      npx tsc --noEmit && \
      npm test -- --silent && \
      npm run build && \
      (git log -1 --pretty=format:'%s' | grep -q '^refactor(03): remove KvBackend dead code + drop KvBackend tests$' || (echo "FAIL: commit message mismatch"; exit 1)) && \
      (git diff --name-only HEAD~1 HEAD | sort | tr '\n' ',' | grep -qE '^(__tests__/anchorStorage.test.ts,src/storage/anchorStorage.ts,?|src/storage/anchorStorage.ts,__tests__/anchorStorage.test.ts,?)$' || (echo "FAIL: commit touches unexpected files"; git diff --name-only HEAD~1 HEAD; exit 1)) && \
      echo "PASS: Task 1"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'KvBackend' src/storage/anchorStorage.ts` returns `0`
    - `grep -c 'createKvBackedAnchorStorage' src/storage/anchorStorage.ts` returns `0`
    - `grep -c 'createKvBackedAnchorStorage' __tests__/anchorStorage.test.ts` returns `0`
    - `grep -c 'fakeKv' __tests__/anchorStorage.test.ts` returns `0`
    - `grep -q 'export const createMemoryAnchorStorage' src/storage/anchorStorage.ts` succeeds (load-bearing export preserved)
    - `grep -q 'export const getDefaultAnchorStorage' src/storage/anchorStorage.ts` succeeds (load-bearing export preserved)
    - `grep -q 'export const __resetDefaultAnchorStorageForTest' src/storage/anchorStorage.ts` succeeds (test-only escape hatch preserved)
    - `grep -q "describe('memory storage'" __tests__/anchorStorage.test.ts` succeeds (5-test block preserved)
    - `grep -q "describe('getDefaultAnchorStorage'" __tests__/anchorStorage.test.ts` succeeds (2-test block preserved)
    - `grep -rn 'createKvBackedAnchorStorage' src/ index.js App.tsx __tests__/` returns no matches (no orphaned consumer)
    - `npx eslint src/ App.tsx index.js __tests__/` exits 0 with no output
    - `npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts"` exits 0 with `All matched files use Prettier code style!`
    - `npx tsc --noEmit` exits 0 with no diagnostics under strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes
    - `npm test` exits 0 with jest reporting `Tests:` count strictly less than 87 AND at least 70 (sanity guard against accidental over-deletion) — exact final count captured for the SUMMARY
    - `npm run build` exits 0 and produces `build/outputs/SnAlign.snplg`
    - `git log -1 --pretty=format:'%s'` outputs exactly `refactor(03): remove KvBackend dead code + drop KvBackend tests` (per D-16)
    - `git diff --name-only HEAD~1 HEAD` outputs exactly two paths: `src/storage/anchorStorage.ts` and `__tests__/anchorStorage.test.ts` (in any order) — no unexpected file leakage (executor may additionally include the `ANCHOR_STORAGE_KEY`-cleanup edits in the same two files per Step 1f; that's still just two paths)
  </acceptance_criteria>
  <done>
    The KvBackend type and `createKvBackedAnchorStorage` factory are gone from `src/storage/anchorStorage.ts`; the 10 KvBackend tests are gone from `__tests__/anchorStorage.test.ts`; `createMemoryAnchorStorage` and `getDefaultAnchorStorage` are intact; all 5 CI gates pass; the refactor lands as a single commit titled exactly `refactor(03): remove KvBackend dead code + drop KvBackend tests`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Docs — record ADOPT-01/02 decline verdicts in REQUIREMENTS, PROJECT, and SUMMARY</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md</files>
  <read_first>
    - `/home/ricardo/repo/SN/sn-align-plugin/.planning/REQUIREMENTS.md` (full file — confirm current ADOPT-01 / ADOPT-02 lines start with `- [ ] **ADOPT-0X**:` and live under the §"Adopt — pick up newly-unlocked capabilities where high value" header at line 18)
    - `/home/ricardo/repo/SN/sn-align-plugin/.planning/PROJECT.md` (full file — confirm Key Decisions table starts at line 77; the existing "Adopt high-value wins from lib upgrade" row is line 87 with Outcome `— Pending`; the "Code-ready milestone (no v0.4.0 tag yet)" row is line 88)
    - `/home/ricardo/repo/SN/sn-align-plugin/.planning/phases/03-adopt-high-value-wins/03-CONTEXT.md` §decisions D-01..D-17 (the entire decision record — Task 2 transcribes the verdicts)
    - `/home/ricardo/repo/SN/sn-align-plugin/.planning/research/lib-0.1.19-to-0.1.43-audit.md` §6.1, §6.2, §6.3, §6.4 (the dispositive evidence each verdict cites)
    - `/home/ricardo/repo/SN/sn-align-plugin/.planning/phases/02-compatibility-upgrade/02-01-SUMMARY.md` (Phase 2 SUMMARY shape — frontmatter format, body sections, "Self-Check: PASSED" tail. Phase 3 SUMMARY mirrors this shape.)
    - `/home/ricardo/repo/SN/sn-align-plugin/.claude/get-shit-done/templates/summary.md` (canonical SUMMARY template if present; otherwise mirror 02-01-SUMMARY.md's structure)
  </read_first>
  <action>
    Three coordinated doc edits + one new SUMMARY file. All three changes land in ONE commit (per D-16 commit 2). The exact wording of verdict notes / PROJECT.md row text is Claude's discretion per CONTEXT.md, subject to the structural / citation requirements below.

    Step 1 — Edit `.planning/REQUIREMENTS.md`. Two checkbox flips per D-13 format.

    1a. Find the line: `- [ ] **ADOPT-01**: If the new lib enables real persistent storage (bundled AsyncStorage native module OR a new KV API on \`PluginManager\`/\`NativePluginManager\`/\`PluginFileAPI\`), swap the in-memory \`anchorStorage\` for it through the existing \`KvBackend\` interface. Anchor + config survive plugin reinstall and device restart, verified by sideload test. **Conditional on UPGRADE-05 finding.**`
        Replace with the same line but `[ ]` → `[x]` AND append a verdict note in italics. Use this template (executor may polish phrasing — must keep audit §6.1 citation AND reopener trigger from D-02):
        ```
        - [x] **ADOPT-01**: If the new lib enables real persistent storage (bundled AsyncStorage native module OR a new KV API on `PluginManager`/`NativePluginManager`/`PluginFileAPI`), swap the in-memory `anchorStorage` for it through the existing `KvBackend` interface. Anchor + config survive plugin reinstall and device restart, verified by sideload test. **Conditional on UPGRADE-05 finding.** — *Declined Phase 3 (2026-05-17): audit §6.1 dispositive — no AsyncStorage bundling in `node_modules/sn-plugin-lib/android/`; empty `dependencies` in `node_modules/sn-plugin-lib/package.json`; no KV-shaped public method on `PluginManager.d.ts` / `NativePluginManager.d.ts` / `PluginFileAPI.d.ts` / `PluginCommAPI.d.ts`. In-memory storage remains the contract (matches ROADMAP §3 SC#1's "If unavailable, the decision is documented and in-memory storage remains"). Dead `KvBackend` scaffolding removed in Phase 3-01 refactor. Reopener: future `sn-plugin-lib` release adds a public KV API on `PluginManager` / `NativePluginManager` / `PluginFileAPI` — re-run the Phase 1 audit shape on any future patch/minor/major release.*
        ```

    1b. Find the line: `- [ ] **ADOPT-02**: If the new lib exposes any API that materially simplifies code we already have (e.g. a working \`modifyButtonRes\`, a built-in page-bounds query, a \`setLassoBoxState\` shortcut), adopt where the simplification is net-negative LOC. Skip if marginal. **Conditional on UPGRADE-05 finding.**`
        Replace with the same line but `[ ]` → `[x]` AND append a verdict note. Use this template (executor may polish — must cite audit §6.2 + §6.3 + §5 N-01/N-03/N-04 + §6.4 AND list all 5 declined candidates):
        ```
        - [x] **ADOPT-02**: If the new lib exposes any API that materially simplifies code we already have (e.g. a working `modifyButtonRes`, a built-in page-bounds query, a `setLassoBoxState` shortcut), adopt where the simplification is net-negative LOC. Skip if marginal. **Conditional on UPGRADE-05 finding.** — *Declined Phase 3 (2026-05-17): all 5 candidate simplifications declined as paper-dispositive. (1) `modifyButtonRes` — audit §6.2: public `PluginManager` wrapper still does not bridge it; the optional `modifyButtonRes?` stub in `src/buttons/buttonCommon.ts` stays as accurate SDK posture. (2) Page-bounds query — audit §6.3: no new public API on `PluginCommAPI` / `PluginFileAPI`; existing 3-step `resolvePageSize` + 1920×2560 fallback in `src/sdk/pageSize.ts` stays. (3) `lassoElements(rect)` — audit §5 N-01 + native bridge inspection (`CommAPIModule.java:763`): semantically distinct from `resizeLassoRect` ("套索元素" = "lasso the elements at this rect", vs. "调整套索框区域大小" = "adjust lasso box region size") — would regress the move-via-bbox trick, not simplify it. (4) `generateLassoPreview` — audit §5 N-03 + §6.4: UX-additive only, no LOC reduction; declined sight-unseen. (5) `showPluginView` — audit §5 N-04 + §6.4: only useful if "Apply without closing" UX is desired; declined sight-unseen. Reopener: a future `sn-plugin-lib` release that bridges `modifyButtonRes` on the public `PluginManager` surface, OR a user signal for lasso-content preview thumbnail (re-evaluates N-03), OR a UX revisit of "Apply without closing" (re-evaluates N-04).*
        ```

    1c. Update the Traceability table (lines 60-73). Find:
        ```
        | ADOPT-01 | Phase 3 (Adopt High-Value Wins) | Pending |
        | ADOPT-02 | Phase 3 (Adopt High-Value Wins) | Pending |
        ```
        Replace `Pending` → `Declined` in both rows.

    1d. Update the "Last updated" footer (line 82) to reflect the Phase 3 closure date `2026-05-17` (today, per the conversation's date field) and a brief tag like "after Phase 3 decision record".

    Step 2 — Edit `.planning/PROJECT.md` per D-14. Two rows in the Key Decisions table.

    2a. Find the existing row at line 87 (pre-edit):
        `| Adopt high-value wins from lib upgrade | If the new lib unlocks deferred behavior (persistent storage, working \`modifyButtonRes\`), pick it up in this milestone rather than waiting. | — Pending |`
        Flip the Outcome column from `— Pending` to a "✓ Declined" verdict per D-14. Executor wording (must contain `✓ Declined`, the dispositive trigger, and the phase reference):
        `| Adopt high-value wins from lib upgrade | If the new lib unlocks deferred behavior (persistent storage, working \`modifyButtonRes\`), pick it up in this milestone rather than waiting. | ✓ Declined — paper-dispositive (Phase 3, audit §6.1: no AsyncStorage bundling / no new KV API). In-memory storage remains; \`KvBackend\` dead code removed in 03-01 refactor. Reopener: future lib release adds a KV API. |`

    2b. Add a second new row immediately after the row above (or grouped with the other "Adopt" verdicts — executor's call on placement, but must be in the Key Decisions table). Executor wording (must contain `✓ Declined`, the candidate count, the dispositive trigger, and the phase reference):
        `| Adopt code simplifications from lib upgrade | If the new lib exposes APIs that materially simplify existing code (modifyButtonRes, page-bounds query, lassoElements, generateLassoPreview, showPluginView), adopt those with net-negative LOC. | ✓ Declined — all 5 candidates paper-dispositive (Phase 3, audit §6.2/§6.3/§5 N-01/N-03/N-04/§6.4). No source edits; \`modifyButtonRes?\` stub stays as optional, accurate SDK posture. Reopener: lib bridges modifyButtonRes publicly, OR user signals demand for preview/"apply-without-closing" UX. |`

    2c. Update the PROJECT.md footer (line 108): change the "Last updated:" date and note to reflect Phase 3 closure (e.g., `2026-05-17 after Phase 3 (Adopt High-Value Wins) recorded ADOPT-01 + ADOPT-02 decline verdicts`).

    Step 3 — Write a NEW file `.planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md`. Mirror the shape of `.planning/phases/02-compatibility-upgrade/02-01-SUMMARY.md`. Required sections (executor controls prose):

    3a. YAML frontmatter (mirror Phase 2 SUMMARY's frontmatter fields):
        ```yaml
        ---
        phase: 03-adopt-high-value-wins
        plan: 01
        subsystem: storage-cleanup, decision-record
        tags: [chore, yagni-cleanup, adopt-decline, decision-record, two-commit-shape]
        requires:
          - "Phase 1: API diff audit (lib-0.1.19-to-0.1.43-audit.md §6.1, §6.2, §6.3, §6.4, §5)"
          - "Phase 2: 87/87 jest baseline on dev (02-01-SUMMARY.md)"
        provides:
          - "ADOPT-01 verdict recorded — declined, in-memory storage remains; KvBackend dead code removed"
          - "ADOPT-02 verdict recorded — all 5 candidates declined paper-dispositive"
          - "Phase 4 planner handoff — audit §8.2, §8.4, §8.5, §8.6 dropped from Phase 4 scope"
        affects:
          - src/storage/anchorStorage.ts
          - __tests__/anchorStorage.test.ts
          - .planning/REQUIREMENTS.md
          - .planning/PROJECT.md
        decisions:
          - "D-01 applied: ADOPT-01 declined per audit §6.1 dispositive"
          - "D-04 applied: KvBackend type + createKvBackedAnchorStorage removed as dead code"
          - "D-05 applied: 10 KvBackend tests removed; test count 87 → <actual_count>"
          - "D-06..D-10 applied: ADOPT-02's 5 candidates all declined paper-dispositive"
          - "D-13 applied: REQUIREMENTS.md [x] Declined format with verdict notes"
          - "D-14 applied: PROJECT.md Key Decisions two rows updated/added"
          - "D-16 applied: two atomic commits — refactor first, docs second"
        metrics:
          commits: 2
          jest_tests_before: 87
          jest_tests_after: <actual_count_from_task_1>
          files_changed_vs_dev: 5
          completed_date: 2026-05-17
        ---
        ```

    3b. Body section "What Was Built" — describe the two commits (per D-16 ordering):
        - Commit 1 `refactor(03): remove KvBackend dead code + drop KvBackend tests` — list the exact regions removed from `src/storage/anchorStorage.ts` (the `KvBackend` type, the `createKvBackedAnchorStorage` factory function, and — if dropped per Task 1 Step 1f — the `ANCHOR_STORAGE_KEY` const + the test-file import). List the exact regions removed from `__tests__/anchorStorage.test.ts` (the `fakeKv` helper, the 10 `it(...)` tests under `describe('KV-backed storage', ...)`). State the jest count change (87 → <actual_count>).
        - Commit 2 `docs(03): record ADOPT-01/02 decline verdicts` — list the three doc files touched: `.planning/REQUIREMENTS.md` (two checkbox flips + traceability table verdicts), `.planning/PROJECT.md` (one row Outcome flip + one new row), and `.planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md` (this file).

    3c. Body section "Decision Record" — for each declined item, give: requirement ID (ADOPT-01 / ADOPT-02 sub-items), dispositive audit subsection (§6.1, §6.2, §6.3, §5 N-01, §5 N-03, §5 N-04, §6.4), CONTEXT.md decision ID (D-01, D-06..D-10), the reopener trigger (D-02 for ADOPT-01; D-08 / D-09 / D-10 reopener clauses for ADOPT-02). The section MUST cite at least four `audit §6.X` cross-references (§6.1, §6.2, §6.3, §6.4) per the quality gate.

    3d. Body section "CI Gate Evidence" — table mirroring 02-01-SUMMARY.md §"CI Gate Evidence (D-05 — real branch)" but with the 5 gates captured at HEAD after Commit 1. (Commit 2 doesn't change source code, so the gates only need to be captured once — at the end of Commit 1 — and that capture also represents the state of HEAD after Commit 2.)

    3e. Body section "Test Count Change" — explicitly note the 87 → <actual_count> drop is intentional per D-05, list the 10 removed test names from `__tests__/anchorStorage.test.ts` pre-refactor lines 76, 88, 95, 101, 108, 115, 129, 143, 155, 167 (these are the 10 `it(...)` titles inside the now-deleted `describe('KV-backed storage', ...)` block), and confirm the surviving counts: 5 `memory storage` tests + 2 `getDefaultAnchorStorage` tests = 7 tests in `anchorStorage.test.ts` post-refactor.

    3f. Body section "Phase 4 Planner Handoff" — per D-03, D-08, D-09, D-10, D-11. Explicitly call out:
        - Audit §8.2 (AsyncStorage runtime probe) is DROPPED from Phase 4 scope per D-03 (paper-evidence-trumps-runtime-check; the long-term reopener in D-02 is the right trigger, not a defensive one-time probe).
        - Audit §8.4 (`lassoElements` test plan) is DROPPED per D-08 (paper-decline based on native bridge JSDoc).
        - Audit §8.5 (`generateLassoPreview` test plan) is DROPPED per D-09 (sight-unseen decline; no LOC reduction).
        - Audit §8.6 (`showPluginView` test plan) is DROPPED per D-10 (sight-unseen decline; tied to undecided UX question).
        - Phase 4 scope reverts to original per D-11 — UPGRADE-04 (sideload-verify v0.3.0 still works) + SKILL-01..05 (skill file updates). No "needs-eval spike" task.
        - Phase 4 should ALSO know: `~/.claude/skills/sn-plugin/references/storage.md` will need a `SKILL-03` edit to remove the SnAlign-side `KvBackend` example after Phase 3-01's `D-04` removal. (Phase 3 does NOT touch the skill file.)

    3g. Body section "Hand-off" — closing note that this plan does NOT include a checkpoint (autonomous: true); the user / orchestrator handles branch creation, push, and PR opening per `.claude/skills/git/SKILL.md` and CONTEXT.md D-15 (branch: `chore/ricardo/03-adopt-decisions`; target: `dev`; PR title follows Phase 2 precedent).

    3h. Body section "Self-Check: PASSED" — mirror Phase 2's tail. List the files created/modified with one-line evidence each.

    Step 4 — Stage and commit. After all three doc edits + SUMMARY write, stage the four files and commit:
        ```
        git add .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md
        git commit -m "docs(03): record ADOPT-01/02 decline verdicts"
        ```
        Commit message verbatim per D-16.

    Step 5 — Re-run the 5 CI gates one more time as a final sanity check (the docs commit doesn't touch source, so the gates should all be no-ops vs. Task 1's capture, but `npm test` / `npm run build` should still exit 0). Capture the final commit graph:
        ```
        git log --oneline dev..HEAD
        ```
        Expected output (2 commits, newest first):
        ```
        <sha2> docs(03): record ADOPT-01/02 decline verdicts
        <sha1> refactor(03): remove KvBackend dead code + drop KvBackend tests
        ```
  </action>
  <verify>
    <automated>
      cd /home/ricardo/repo/SN/sn-align-plugin && \
      (grep -E '^- \[x\] \*\*ADOPT-01\*\*' .planning/REQUIREMENTS.md > /dev/null || (echo "FAIL: ADOPT-01 not flipped to [x]"; exit 1)) && \
      (grep -E '^- \[x\] \*\*ADOPT-02\*\*' .planning/REQUIREMENTS.md > /dev/null || (echo "FAIL: ADOPT-02 not flipped to [x]"; exit 1)) && \
      (grep -E '\*Declined Phase 3' .planning/REQUIREMENTS.md | wc -l | awk '$1>=2 {exit 0} {exit 1}' || (echo "FAIL: REQUIREMENTS.md missing 2 'Declined Phase 3' verdict notes"; exit 1)) && \
      (grep -c '§6\.1' .planning/REQUIREMENTS.md | awk '$1>=1 {exit 0} {exit 1}' || (echo "FAIL: REQUIREMENTS.md missing audit §6.1 citation (ADOPT-01)"; exit 1)) && \
      (grep -cE '§6\.2|§6\.3|§6\.4|N-01|N-03|N-04' .planning/REQUIREMENTS.md | awk '$1>=4 {exit 0} {exit 1}' || (echo "FAIL: REQUIREMENTS.md ADOPT-02 missing audit citations"; exit 1)) && \
      (grep -c 'Reopener' .planning/REQUIREMENTS.md | awk '$1>=2 {exit 0} {exit 1}' || (echo "FAIL: REQUIREMENTS.md missing reopener triggers"; exit 1)) && \
      (! grep -E '^\| ADOPT-0[12] \| Phase 3 \(Adopt High-Value Wins\) \| Pending \|' .planning/REQUIREMENTS.md > /dev/null || (echo "FAIL: Traceability table still shows Pending for ADOPT-01/02"; exit 1)) && \
      (grep -c '✓ Declined' .planning/PROJECT.md | awk '$1>=2 {exit 0} {exit 1}' || (echo "FAIL: PROJECT.md needs ≥2 '✓ Declined' rows"; exit 1)) && \
      (grep -c 'paper-dispositive' .planning/PROJECT.md | awk '$1>=1 {exit 0} {exit 1}' || (echo "FAIL: PROJECT.md missing 'paper-dispositive' rationale"; exit 1)) && \
      (grep -c 'Adopt code simplifications' .planning/PROJECT.md | awk '$1>=1 {exit 0} {exit 1}' || (echo "FAIL: PROJECT.md missing ADOPT-02 row"; exit 1)) && \
      test -f .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md && \
      (wc -l < .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md | awk '$1>=60 {exit 0} {exit 1}' || (echo "FAIL: SUMMARY too short"; exit 1)) && \
      (grep -c 'audit §6\.' .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md | awk '$1>=4 {exit 0} {exit 1}' || (echo "FAIL: SUMMARY needs ≥4 'audit §6.' cross-refs (§6.1, §6.2, §6.3, §6.4 at minimum)"; exit 1)) && \
      (grep -E 'audit §8\.(2|4|5|6)' .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md > /dev/null || (echo "FAIL: SUMMARY missing Phase 4 §8.x handoff"; exit 1)) && \
      (grep -E '87 → ' .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md > /dev/null || (echo "FAIL: SUMMARY missing test count change '87 → ...'"; exit 1)) && \
      npx eslint src/ App.tsx index.js __tests__/ && \
      npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts" && \
      npx tsc --noEmit && \
      npm test -- --silent && \
      npm run build && \
      (git log -1 --pretty=format:'%s' | grep -q '^docs(03): record ADOPT-01/02 decline verdicts$' || (echo "FAIL: latest commit message mismatch"; exit 1)) && \
      (test "$(git rev-list --count dev..HEAD)" -eq 2 || (echo "FAIL: expected 2 commits ahead of dev"; git log --oneline dev..HEAD; exit 1)) && \
      (git log -2 --pretty=format:'%s' | tr '\n' '|' | grep -qE '^docs\(03\): record ADOPT-01/02 decline verdicts\|refactor\(03\): remove KvBackend dead code \+ drop KvBackend tests$' || (echo "FAIL: commit order incorrect (per D-16 refactor must precede docs)"; git log -2 --oneline; exit 1)) && \
      (git diff --name-only HEAD~1 HEAD | sort | tr '\n' ',' | grep -qE 'PROJECT.md,.*REQUIREMENTS.md,.*03-01-SUMMARY.md' || (echo "FAIL: docs commit touches unexpected files"; git diff --name-only HEAD~1 HEAD; exit 1)) && \
      echo "PASS: Task 2"
    </automated>
  </verify>
  <acceptance_criteria>
    REQUIREMENTS.md:
    - `grep -E '^- \[x\] \*\*ADOPT-01\*\*' .planning/REQUIREMENTS.md` returns a match (checkbox flipped)
    - `grep -E '^- \[x\] \*\*ADOPT-02\*\*' .planning/REQUIREMENTS.md` returns a match (checkbox flipped)
    - REQUIREMENTS.md contains the substring `Declined Phase 3` at least twice (one per requirement)
    - REQUIREMENTS.md contains `§6.1` (ADOPT-01 audit citation) at least once
    - REQUIREMENTS.md contains at least 4 of the citation tokens `§6.2`, `§6.3`, `§6.4`, `N-01`, `N-03`, `N-04` (ADOPT-02 enumerates 5 candidates)
    - REQUIREMENTS.md contains the word `Reopener` at least twice (per D-13's reopener-trigger requirement)
    - Traceability table no longer contains `| ADOPT-01 | Phase 3 (Adopt High-Value Wins) | Pending |` or `| ADOPT-02 | Phase 3 (Adopt High-Value Wins) | Pending |` (Status flipped to Declined)

    PROJECT.md:
    - `grep -c '✓ Declined' .planning/PROJECT.md` returns ≥ 2 (one row updated, one new row)
    - PROJECT.md contains `paper-dispositive` at least once
    - PROJECT.md Key Decisions table contains a row with cell text `Adopt code simplifications` (new ADOPT-02 row per D-14)
    - PROJECT.md Key Decisions table no longer contains the literal cell `— Pending` against `Adopt high-value wins from lib upgrade` (flipped per D-14)

    SUMMARY.md:
    - File `.planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md` exists
    - SUMMARY has ≥ 60 lines (substantive decision record, not a stub)
    - `grep -c 'audit §6\.' .planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md` returns ≥ 4 (cites §6.1, §6.2, §6.3, §6.4 at minimum)
    - SUMMARY contains substrings matching `audit §8.2`, `audit §8.4`, `audit §8.5`, `audit §8.6` (Phase 4 handoff per D-03/D-08/D-09/D-10/D-11) — `grep -E 'audit §8\.(2|4|5|6)'` matches at least once
    - SUMMARY contains the test-count change pattern `87 → ` (with the actual post-refactor count after the arrow)
    - SUMMARY references commits in order: `refactor(03)` line precedes `docs(03)` line

    CI gates (re-run at end of Task 2):
    - `npx eslint src/ App.tsx index.js __tests__/` exits 0
    - `npx prettier --check ...` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 with the same `Tests:` count captured at end of Task 1
    - `npm run build` exits 0

    Git state:
    - `git log -1 --pretty=format:'%s'` outputs exactly `docs(03): record ADOPT-01/02 decline verdicts` (per D-16)
    - `git rev-list --count dev..HEAD` returns exactly `2`
    - `git log -2 --pretty=format:'%s'` (newest first) outputs `docs(03): record ADOPT-01/02 decline verdicts` then `refactor(03): remove KvBackend dead code + drop KvBackend tests` (per D-16 ordering)
    - `git diff --name-only HEAD~1 HEAD` contains exactly the three docs paths: `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md`
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md ADOPT-01 + ADOPT-02 are flipped `[x]` Declined with verdict notes citing audit subsections and reopener triggers per D-13; PROJECT.md Key Decisions table has one updated row (ADOPT-01 Outcome flipped to `✓ Declined`) and one new row (ADOPT-02) per D-14; `03-01-SUMMARY.md` exists and documents the decision record + audit cross-references + test count change + Phase 4 planner handoff; the docs land as a single commit titled exactly `docs(03): record ADOPT-01/02 decline verdicts`; `git rev-list --count dev..HEAD` = 2; the 5 CI gates remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan crosses NO trust boundaries:
- No new code accepts external input.
- No new code crosses the React Native ↔ firmware bridge.
- No new SDK call sites are added (`sn-plugin-lib` integration is read-only context).
- No new file I/O or network I/O.
- Source-tree edits only REMOVE code (`KvBackend` type, `createKvBackedAnchorStorage` factory, 10 KV-backed tests) — strictly reduces attack surface.
- All other edits are planning markdown files (`REQUIREMENTS.md`, `PROJECT.md`, `03-01-SUMMARY.md`).

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase removes dead in-memory storage scaffolding and edits planning markdown. No new trust boundary is established or crossed. |

## STRIDE Threat Register

No security-relevant changes — removing dead in-memory storage stub and editing planning markdown only. The `KvBackend` factory being removed never had a live consumer (verified by `grep -rn 'createKvBackedAnchorStorage' src/ index.js App.tsx` returning zero matches outside `anchorStorage.ts` itself), so the removal cannot regress an existing security posture. The surviving `createMemoryAnchorStorage` carries forward the same in-memory-only contract that the project has shipped under since v0.2.0 (already declared in `PROJECT.md` §Constraints as a known accepted limitation).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | (none) | n/a | n/a | No security-relevant changes — removing dead in-memory storage stub and editing planning markdown only. No input handling change, no SDK boundary change, no persistence change (in-memory contract unchanged), no auth surface, no new dependency. The phase strictly removes code and adds documentation. |

## Package legitimacy gate

Not applicable — this phase does NOT install or modify any npm packages. `package.json` and `package-lock.json` are untouched. The audit's package legitimacy posture established in Phase 2 carries forward unchanged.
</threat_model>

<verification>
After both tasks complete and both commits land, the following overall phase checks must hold:

1. **Source surface contracted, not expanded.** `git diff --name-only dev HEAD -- src/` returns exactly `src/storage/anchorStorage.ts`. No other source file is touched. No `App.tsx`, `index.js`, no `src/handlers/`, `src/buttons/`, `src/sdk/`, `src/ui/`, `src/i18n/`, `src/core/`. Phase boundary holds.

2. **Test surface contracted.** `git diff --name-only dev HEAD -- __tests__/` returns exactly `__tests__/anchorStorage.test.ts`. No other test file is touched.

3. **Public exports preserved.** `grep -c 'export ' src/storage/anchorStorage.ts` returns the same count as pre-refactor MINUS exactly 1 (the removed `createKvBackedAnchorStorage` export) and optionally MINUS 1 more if `ANCHOR_STORAGE_KEY` was dropped. The exports `createMemoryAnchorStorage`, `getDefaultAnchorStorage`, `__resetDefaultAnchorStorageForTest`, `DEFAULT_ANCHOR_STATE`, and the `AnchorStorage` / `AnchorState` / `AnchorEnvelope` types all survive.

4. **index.js wiring unbroken.** `grep -n 'getDefaultAnchorStorage' index.js` matches (the load-bearing entry point). The build succeeds (`npm run build` exit 0), implying the bundle resolves all imports.

5. **Orthogonality invariant preserved.** The surviving `createMemoryAnchorStorage` + `buildStorage` implementation in `src/storage/anchorStorage.ts` keeps the read-modify-write pattern for `setConfig` and `setAnchorBox` (each touches only its own slot — `config` ⟂ `anchorBox`). Verify with `grep -A 5 'async setConfig' src/storage/anchorStorage.ts` showing the spread `{...current, config}` pattern (no coupled write), and `grep -A 5 'async setAnchorBox' src/storage/anchorStorage.ts` showing `{...current, anchorBox: box}` (no coupled write). ROADMAP §3 SC#3 satisfied.

6. **Audit cross-references complete.** Phase 3 closure cites every audit subsection that drove a verdict: §6.1 (D-01), §6.2 (D-06), §6.3 (D-07), §5 N-01 + §6.4 (D-08), §5 N-03 (D-09), §5 N-04 (D-10). All present in REQUIREMENTS.md + SUMMARY.md per the per-task acceptance criteria.

7. **Phase 4 planner is informed.** `03-01-SUMMARY.md` "Phase 4 Planner Handoff" section explicitly mentions audit §8.2, §8.4, §8.5, §8.6 are dropped from Phase 4 scope per D-03/D-08/D-09/D-10/D-11. The Phase 4 planner reads this when invoked.

8. **All 5 CI gates green at HEAD.** Re-run after Task 2's commit:
   - `npx eslint src/ App.tsx index.js __tests__/` → exit 0
   - `npx prettier --check "src/**/*.{ts,tsx}" "App.tsx" "index.js" "__tests__/**/*.ts"` → exit 0
   - `npx tsc --noEmit` → exit 0
   - `npm test` → exit 0, `Tests:` count strictly less than 87 (per D-05) and at least 70 (sanity floor)
   - `npm run build` → exit 0, `build/outputs/SnAlign.snplg` produced

9. **Commit graph matches D-16.** `git log --oneline dev..HEAD` shows exactly 2 commits, newest first:
   - `<sha> docs(03): record ADOPT-01/02 decline verdicts`
   - `<sha> refactor(03): remove KvBackend dead code + drop KvBackend tests`
</verification>

<success_criteria>
Phase 3 (Adopt High-Value Wins) is complete when:

1. **ADOPT-01 verdict recorded** (ROADMAP §3 SC#1) — `.planning/REQUIREMENTS.md` has `- [x] **ADOPT-01**:` followed by a verdict note citing audit §6.1 and the reopener trigger (D-02). In-memory storage remains via `createMemoryAnchorStorage`; the `KvBackend` dead code is removed.

2. **ADOPT-02 verdict recorded** (ROADMAP §3 SC#2) — `.planning/REQUIREMENTS.md` has `- [x] **ADOPT-02**:` followed by a verdict note enumerating all 5 declined candidates and citing audit §6.2, §6.3, §5 N-01, §5 N-03, §5 N-04, §6.4 with reopener triggers.

3. **Orthogonality invariant preserved** (ROADMAP §3 SC#3) — `createMemoryAnchorStorage` + `buildStorage` retain the read-modify-write pattern; `setConfig` touches only `config`, `setAnchorBox` touches only `anchorBox` (verified by `grep` against the post-refactor file).

4. **Tests and CI green** (ROADMAP §3 SC#4) — `npm test` exit 0 with count strictly less than 87 (per D-05's intentional drop), `npx tsc --noEmit` exit 0, `npx eslint` exit 0, `npx prettier --check` exit 0, `npm run build` exit 0.

5. **PROJECT.md Key Decisions reflects Phase 3 outcomes** (per D-14) — existing "Adopt high-value wins from lib upgrade" row Outcome column flipped from `— Pending` to `✓ Declined — paper-dispositive (Phase 3, ...)`; new row added: "Adopt code simplifications from lib upgrade" with `✓ Declined — all 5 candidates paper-dispositive (Phase 3, ...)`.

6. **03-01-SUMMARY.md is the closure artifact** (per D-12) — file exists, documents the two-commit refactor + docs shape (D-16), cross-references every dispositive audit subsection (≥ 4 `audit §6.X` references), explicitly hands off audit §8.2/§8.4/§8.5/§8.6 removals to the Phase 4 planner (D-03/D-08/D-09/D-10/D-11), and states the test count change (87 → <actual_count>).

7. **Two atomic commits land in D-16 order** — `git log --oneline dev..HEAD` returns exactly 2 commits; refactor commit (`refactor(03): ...`) precedes docs commit (`docs(03): ...`).

8. **Phase boundary holds** — only 5 paths in `git diff --name-only dev HEAD`: 2 source-tree files (`src/storage/anchorStorage.ts`, `__tests__/anchorStorage.test.ts`) and 3 planning-tree files (`.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `.planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md`).
</success_criteria>

<output>
Create `.planning/phases/03-adopt-high-value-wins/03-01-SUMMARY.md` per Task 2 Step 3. The SUMMARY is the post-execution closure artifact; its frontmatter records `commits: 2`, `jest_tests_before: 87`, `jest_tests_after: <actual>`, `files_changed_vs_dev: 5`, `completed_date: 2026-05-17`. Body sections mirror Phase 2's 02-01-SUMMARY.md shape (frontmatter → What Was Built → Decision Record → CI Gate Evidence → Test Count Change → Phase 4 Planner Handoff → Hand-off → Self-Check: PASSED).

After both tasks complete, the executor stops here. The orchestrator handles branch creation (`chore/ricardo/03-adopt-decisions` per D-15), push, and PR opening per `.claude/skills/git/SKILL.md` and Phase 2 precedent.
</output>
